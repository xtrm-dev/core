# Lane 1 — Core-owned runtime enforcement and session lifecycle audit

Read-only architecture audit. No source, config, or runtime state was modified. The only file created is this report.

## Baseline (re-verified in this worktree)

| Fact | Value | How verified |
|---|---|---|
| Core revision | `e3c09927f115a9ad551953ede2262c91a9bbb431` (2026-09-12), package `xtrm-tools@0.12.0` | `git log -1 --format='%H %ad %s'`; `package.json` |
| Working tree | clean | `git status --porcelain` (empty) |
| Substrate upstream | `12e71d743a32c7b27af6c3e792574cfa08e7b81a`, snapshot `/tmp/audit/xtrm-main` | prompt baseline; snapshot read directly |
| Specialists upstream | `67100f1362c01f0972e081795be805297be8e8a9`, snapshot `/tmp/audit/specialists-master` | prompt baseline; snapshot read directly |
| Local runtime state | `sb` on PATH (`/home/dawid/.nvm/versions/node/v24.15.0/bin/sb`), `~/.xtrm/state.db` present (17.9 MB) | `command -v sb`; `ls -la ~/.xtrm/state.db` |
| Substrate enrollment (Claude) | **absent** — `~/.claude/plugins/installed_plugins.json` has no `substrate@substrate`; `~/.claude/settings.json` contains **0** occurrences of `substrate` | `python3` parse of both files |
| Substrate enrollment (Pi) | **absent** — `~/.pi/agent/settings.json` `packages[]` has no substrate entry; 0 occurrences of `substrate` | `python3` parse |
| Core gates (Claude, live) | beads-* gates wired in `~/.claude/settings.json` (see F-25) | `python3` parse |

Authority model used for target-owner assignment is taken from the task prompt (Chain Runtime → workflow progression; Specialists → participant runtime; Substrate → durable work/Journal/claims/Closure/ExecutionBinding/provenance/Resume; Channels → coordination; Core → installation/launch/UX/compatibility/composition/façades; Git → code truth).

Statement labelling: every row's `Evidence` column is source-derived (VERIFIED at the cited `path:line`). `Current behavior` is source-derived unless the row says otherwise. Anything marked INFERRED / RECOMMENDATION / UNRESOLVED appears in those rows or in the closing sections.

---

## Surfaces searched

- `packages/pi-extensions/` — `src/index.ts`, `src/registry.ts`, `src/manifest.json`, `src/core/{index,lib,adapter,guard-rules,logger,runner,session-state}.ts`, `src/extensions/*.ts` (11 shims), `extensions/{beads,compact-header,custom-footer,git-checkpoint,python-kernel,quality-gates,read-line-numbers,session-flow,sp-terminal-overlay,xtprompt,xtrm-ui}/index.ts`, all `extensions/*/package.json`.
- `.xtrm/` — `ext-src/**` (28 tracked files), `packages/pi-extensions/**` (46 tracked files), `packages/pi-extensions/src/shared/legacy-path-map.ts`, `hooks/**` (25 tracked files incl. `gitnexus/`), `registry.json` (assets.hooks/skills/config), `config/{hooks.json,settings.json,instructions/*,pi/*}`, `skills/`, `specialists-source.json`, `session-meta.json`, `reports/`.
- `hooks/` (root) — resolved as a symlink, not a directory.
- `policies/*.json` (10 files + `schema.json`) and `scripts/compile-policies.mjs` (incl. `--check-pi` executed read-only).
- `cli/src/core/` — `substrate.ts`, `substrate-migration.ts`, `substrate-verify.ts`, `claude-runtime-sync.ts`, `pi-runtime-hooks.ts`, `pi-runtime.ts`, `global-hooks-bootstrap.ts`, `global-hooks-flag.ts`, `legacy-hook-dedupe.ts`, `plugin-era-cleanup.ts`, `beads-shared-server.ts`, `bd-auto-stage-patch.ts`, `installer-manifest.ts`, `drift.ts`, `init-verification.ts`, `machine-bootstrap.ts`, `registry-scaffold.ts`, `global-prompt-sync.ts`; `cli/src/commands/install.ts`.
- `.claude/` — `hooks/` (3 tracked symlinks), `settings.json` (absent), `rules/`, `git-hooks/`, `docs/`.
- `.githooks/` — `prepare-commit-msg`, `pre-commit`, `pre-push`, post-* and `*.bd-sync` variants.
- Live machine state (read-only): `~/.claude/settings.json`, `~/.pi/agent/settings.json`, `~/.xtrm/hooks/**`, `~/.xtrm/config/hooks.json`, `~/.xtrm/hooks/state.json`, `~/.claude/plugins/{installed_plugins,known_marketplaces}.json`.
- Substrate upstream: `packages/substrate/integrations/{setup.ts,write-and-exit.ts}`, `integrations/pi/{extension.ts,handlers.ts}`, `integrations/claude-code/*.ts` (11 files) + `hooks.json`, `packages/substrate/hooks/{prepare-commit-msg,post-commit,post-rewrite}.ts`, `plugin/substrate/.claude-plugin/plugin.json`, `cli/sb.ts` verb/flag table.
- Specialists upstream: `plugins/specialists/{hooks/hooks.json,.claude-plugin/plugin.json,.mcp.json,scripts/*}`, `config/hooks/specialists-session-start.mjs`, `src/pi/*`, `pi/rpc/*`.

## Not inspected

- `packages/pi-extensions/extensions/xtrm-ui/index.ts` (1751 lines) and `xtprompt/index.ts` (605 lines) were inspected only for registration surface and Beads/`bd` references, not line-by-line.
- `packages/pi-extensions/extensions/{sp-terminal-overlay,python-kernel,read-line-numbers,compact-header}` beyond registration surface, `bd`/Substrate references, and the `preflight` helper.
- `.xtrm/skills/**` (210 skill files) and `.xtrm/reports/**` content.
- `cli/src/{commands,utils,adapters}` outside `install.ts` and `utils/git-staging.ts`; Lane 2 owns the `xt` CLI lifecycle.
- `.xtrm/registry.json` skill/config asset hashes were not individually re-derived.
- `cli/dist/index.cjs` (built bundle) was not decompiled; findings are against `cli/src`.
- Specialists runtime internals (job lifecycle, KPI, node execution) beyond the Claude plugin/hook surface.
- XTRM Chain Runtime source — no local checkout was provided; Chain claims in this report are INFERENCE from the authority model statement only.
- Core's test suites were not executed except `scripts/compile-policies.mjs --check-pi` (read-only, no writes).

---

## Findings

Action ∈ {KEEP, ADAPT, DELETE, REPLACE, COMPAT} · Confidence ∈ {VERIFIED, INFERRED, OPEN}

### A. Pi extension package (`packages/pi-extensions/`)

| Current surface | Current behavior | Current owner | Target owner | Target primitive | Evidence | Action | Upstream dependency | Ordering | Acceptance proof | Migration hazard | Release impact | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `extensions/beads/index.ts` (`tool_call`) | Blocks any mutating file tool when the session has no `bd kv` claim; blocks `git commit` while a claim is open; spawns `bd kv get`/`bd show` per tool call (run-scoped cache) | Core (Beads) | Substrate | `integrations/pi/handlers.ts:332` `decidePiEditCall` + `integrations/pi/extension.ts:183-192` `pi.on("tool_call")` | `packages/pi-extensions/extensions/beads/index.ts:19-54,88-129`; active+required in `src/manifest.json:4-9` and `src/registry.ts:22` | DELETE | Substrate Pi package enrolled (`integrations/setup.ts:252` `planCommands` → `pi install`) | Substrate Pi enrollment MUST precede removal (see F-25, F-26) | Pi session bound to a Substrate issue with no live claim: edit blocked with a Substrate reason string and zero `bd` subprocesses spawned (`strace`/process count = 0) | This is the **only** live Pi edit gate today; removing it before Substrate is enrolled leaves Pi ungated | MAJOR | VERIFIED |
| `extensions/beads/index.ts` (`tool_result`) | Auto-writes `bd kv set claimed:<session>` on `bd update --claim`; writes `closed-this-session` on `bd close`; injects claim notices into tool output | Core (Beads) | Substrate | `integrations/pi/handlers.ts:151` `toolIssueClaim` + `SessionBindings` (extension.ts:108-110) | `extensions/beads/index.ts:131-172` | DELETE | Substrate claim tool registered (`integrations/pi/extension.ts:96-113`) | Together with Substrate enrollment | `substrate_issue_claim` binds the session; `/substrate-resume` returns a capsule; no `bd kv` keys written | Claim state lives in `bd kv`; deleting without a Substrate claim leaves sessions unbound, which now **fails open** (ungated edits) | MAJOR | VERIFIED |
| `extensions/session-flow/index.ts` (`agent_end`) | Non-blocking stop nudge when the claimed `bd` issue is still in progress; worktree reminder | Core (Beads) | Substrate | `integrations/pi/handlers.ts:390` `evaluateStopContinuity` | `extensions/session-flow/index.ts:67-95`; active+required `src/manifest.json:35-40` | ADAPT → DELETE | **Missing upstream wiring**: `integrations/pi/handlers.ts:390` exports `evaluateStopContinuity` but `integrations/pi/extension.ts:183,203,210` registers only `tool_call`, `session_start`, `session_before_compact` — no `agent_end` handler exists | After upstream adds an `agent_end` wire | Pi `agent_end` emits the five-signal continuity line from Substrate state; no `bd show` spawn | Deleting now removes the only Pi continuity nudge; upstream Claude has an equivalent (`claude-code/stop-gate.ts:1-12`) but upstream Pi does not | MINOR | VERIFIED |
| `extensions/session-flow/index.ts` (`tool_result`) | Injects "Session Flow: claimed <id>" text on `bd update --claim` | Core (Beads) | Substrate | `SessionBindings` bound inside `toolIssueClaim` (`integrations/pi/handlers.ts:161`) | `extensions/session-flow/index.ts:52-63` | DELETE | none | with enrollment | Claim notification originates from the Substrate tool result, not a regex over a `bd` command | none material | PATCH | VERIFIED |
| `extensions/custom-footer/index.ts` | Active+required footer that dynamic-imports `.xtrm/hooks/beads-status-cache.mjs` from the repo/main checkout and renders `o:/p:/b:` counts from `.xtrm/cache/beads-status.json` | Core (Beads projection) | Core façade over Substrate projection | `sb issue list --project <id> --json` / `sb issue state --project <id> --json` (`packages/substrate/cli/sb.ts:795,804`) | `extensions/custom-footer/index.ts:34` (`CACHE_MODULE`), `:133-160` (`loadCacheModule`), `:253-255` (counts) | ADAPT | `sb` CLI verbs exist; no Substrate "display projection" primitive is needed | Independent of gate removal; must precede `beads-status-cache.mjs` deletion | Footer renders live Substrate counts with no Beads module import and no `bd` spawn | Runtime `import()` of a hook file that is **not** in `registry.json`; if the Beads module is deleted first, the footer silently degrades (fails closed to empty counts) | MINOR | VERIFIED |
| `extensions/quality-gates/index.ts` | Disabled in manifest; would resolve `.claude/hooks/quality-check.cjs` / `.py` under `cwd` | Core | Core (path bug) | `.xtrm/hooks/quality-check.cjs` / `.py` (canonical, registry-listed) | `extensions/quality-gates/index.ts:6-16`; disabled reason `src/manifest.json:67`; canonical path `.xtrm/hooks/quality-check.cjs` | DELETE or ADAPT | none | none | `check:pi-manifest` stays green; quality gates fire via Claude hooks as today | None (extension is inert) | PATCH | VERIFIED |
| `extensions/git-checkpoint/index.ts` | `git stash create` per turn; `session_before_fork` offers stash restore | Core | Core (Git/editor UX) | none upstream | `extensions/git-checkpoint/index.ts:15-52` | KEEP | none | none | `/fork` restore prompt appears; no Substrate journal write | none | NONE | VERIFIED |
| `extensions/compact-header/index.ts`, `read-line-numbers`, `xtrm-ui`, `xtprompt`, `sp-terminal-overlay`, `python-kernel` | Pure UX/editor/tooling; no claim or work-authority writes | Core | Core | none | registration surface only (`compact-header/index.ts:9`; `xtrm-ui/index.ts:903-1600`; `xtprompt/index.ts:218,223`; `sp-terminal-overlay/index.ts:454-487`; `python-kernel/index.ts:850,959`; `read-line-numbers/index.ts:81`) | KEEP | none | none | `check:pi-manifest` green; no `bd`/claim write in any of them | none | NONE | VERIFIED |
| `extensions/xtrm-ui/index.ts` | Registers tools named `bash`, `read`, `edit`, `write`, `find`, `grep`, `ls` — same names as native Pi tools | Core | Core | native Pi tools | `extensions/xtrm-ui/index.ts:1341,1372,1421,1459,1533,1566,1599` | ADAPT | none | none | Native and xtrm-ui tool names do not collide, or collision is documented as intentional replacement | INFERENCE: tool-name shadowing is intentional (custom renderers with native `execute`), but a future Pi tool-name change would silently shadow differently. Not proven a gate bypass: gates match `event.toolName` and the shadowing tools keep the same names | PATCH | INFERRED |
| `extensions/python-kernel/index.ts` `preflight()` | Builds a per-file digest from `git log --follow`, pending diff, and **`bd memories <topic>`** | Core | Core, minus the `bd` arm | commit corpus (already in the helper) + `sb journal` entries | `extensions/python-kernel/index.ts:194-199` (`bd memories`), `:855-862` (doctrine strings) | ADAPT | none | none | `preflight()` returns commits + diff; no `bd` subprocess; digest text unchanged when `bd` is absent | `bd memory` stack was retired in `959c7718` ("retire bd-memory stack"); the arm now silently returns empty and the docstrings still advertise it | PATCH | VERIFIED |
| `src/core/session-state.ts` | Exports `SessionState`/`SessionPhase`/`findSessionStateFile`/`readSessionState` for `.xtrm-session-state.json`; **zero callers** in shipped code | Core | — | none | `src/core/session-state.ts:1-59`; repo-wide `readSessionState`/`findSessionStateFile`/`SessionPhase` hits are only this file, `.xtrm/ext-src/core/session-state.ts`, `.xtrm/packages/.../session-state.ts`, and `cli/test/hooks.test.ts:387,433,815,882` (test writes the fixture) | DELETE | none | none | `bun test` in `packages/pi-extensions` and `cli/test/hooks.test.ts` pass after removal | The old closure-phase machine (`claimed`/`waiting-merge`/`conflicting`/`merged`) is exactly the pre-Substrate closure authority; leaving it exported invites a future writer | PATCH | VERIFIED |
| `src/core/guard-rules.ts` `SAFE_BASH_PREFIXES`, `DANGEROUS_BASH_PATTERNS` | Constants listed as "canonical guard-rule constants"; no consumer anywhere in `packages/pi-extensions` | Core | — | none | `src/core/guard-rules.ts:18-99`; grep for both identifiers returns only the definition file | DELETE | none | none | build + tests green | none | PATCH | VERIFIED |
| `src/core/adapter.ts` `parseBdCounts`, `formatBlockReason` | Named helpers for `bd list` summary parsing / reason formatting; no consumer | Core | — | none | `src/core/adapter.ts:33-51`; grep returns only `adapter.ts` | DELETE | none | none | build green | `parseBdCounts` is the residue of `hasTrackableWork`, which `xtrm-64pl0` removed from the edit gate (`extensions/beads/index.ts:93-97`) | PATCH | VERIFIED |
| `src/core/adapter.ts` `isBeadsProject` | Gate precondition: the Pi gates only arm inside a directory containing `.beads` | Core (Beads) | Substrate | `SUBSTRATE_ISSUE_REF` / `.substrate-issue` binding discovery (`integrations/claude-code/runtime.ts:53-66`) | `src/core/adapter.ts:39-41`; used at `extensions/beads/index.ts:90`, `extensions/session-flow/index.ts:55,69` | DELETE | none | with gate removal | Gate decision no longer keys on a `.beads` directory | Verified live: this worktree has **no** `.beads` (`ls -d .beads` empty) while `/home/dawid/dev/core/.beads` exists — the Beads gate is **already inert in worktrees**, so Core's Pi editing enforcement is currently off in worktree sessions even though the manifest marks it required | MAJOR | VERIFIED |
| `src/registry.ts` + `src/manifest.json` | `beads` and `session-flow` are `required: true` and active; the policy compiler simultaneously declares both RETIRED | Core | Core (single source of truth) | `policies/*.json` (compiler) OR `manifest.json`, not both | `src/manifest.json:4-9,35-40`; `policies/beads.json:2-4` ("RETIRED"); `policies/session-flow.json:2-4` ("RETIRED") | ADAPT | none | none | One declaration source; `check:pi-manifest` and `--check-pi` agree on the active set | Two Core-owned config surfaces disagree about whether the same two extensions are shipped requirements | PATCH | VERIFIED |
| `src/shared/legacy-path-map.ts` | Declares `.xtrm/ext-src/*` as `legacyPath` and `packages/pi-extensions/...` as `newPath` | Core | Core | none | `packages/pi-extensions/src/shared/legacy-path-map.ts:7-16` | KEEP (as the deletion justification for B) | none | none | map still referenced by tests after the legacy trees are deleted | none | NONE | VERIFIED |

### B. Legacy extension trees (stale duplicates of the same code)

| Current surface | Current behavior | Current owner | Target owner | Target primitive | Evidence | Action | Upstream dependency | Ordering | Acceptance proof | Migration hazard | Release impact | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `.xtrm/ext-src/**` | 28 tracked files: a pre-fork of every extension importing `@mariozechner/pi-coding-agent` and `@xtrm/pi-core`; behaviour differs from shipped code (older `gate` logic, `hasTrackableWork`, `memoryGateFired`, extra `bd kv` helpers) | Core (legacy) | — | `packages/pi-extensions/extensions/*` | 28 files via `git ls-files .xtrm/ext-src`; `legacy-path-map.ts:7-16`; diff `.xtrm/ext-src/beads/index.ts` vs shipped shows `@mariozechner/pi-coding-agent` + dropped `hasTrackableWork`/`memoryGateFired`; tree includes retired `service-skills` | DELETE | none | after B-tests re-point | `git ls-files .xtrm/ext-src` empty; `packages/pi-extensions/tests/retired-extensions.test.ts:22-63` still passes (it asserts **absence**, never presence) | Only `.xtrm/packages/pi-extensions/src/shared/legacy-path-map.ts` and `cli/test/{install-pi,pi-extensions}.test.ts` mention `ext-src`, and those tests use `os.tmpdir()` prefixes (`pi-ext-src-`), not the tree — no functional consumer found | PATCH | VERIFIED |
| `.xtrm/packages/pi-extensions/**` | 46 tracked files: a second stale copy of `extensions/{beads,compact-header,custom-footer,git-checkpoint,quality-gates,service-skills,session-flow,xtrm-ui}` + `src/{registry,index,core,shared}` | Core (legacy) | — | `packages/pi-extensions/**` | 46 files via `git ls-files .xtrm/packages`; `diff -q` for beads/session-flow/custom-footer all DIFF; tree has no `python-kernel`, `read-line-numbers`, `sp-terminal-overlay`, `xtprompt` (all shipped) | DELETE | none | with B1 | same tree absent; `check:payload-hygiene` still green | `retired-extensions.test.ts:9,43-45` asserts retired ids are absent from this tree — it does **not** require the tree to exist | PATCH | VERIFIED |
| `.xtrm/ext-src/service-skills` and `.xtrm/packages/.../service-skills` | Retired extension retained in both legacy trees | Core (legacy) | — | `npm:@jaggerxtrm/pi-service-knowledge` | `src/manifest.json:68` (disabled reason: "Superseded by service-knowledge (xtrm-6z6.1) … Replaced by the self-gating service-knowledge extension") | DELETE | none | with B1/B2 | both trees absent; manifest unchanged | none | PATCH | VERIFIED |

### C. Hook payload (`.xtrm/hooks/`, `hooks/`, registry, bootstrap)

**Key structural fact (VERIFIED):** `.xtrm/registry.json` `assets.hooks.files` lists **14** files. Eleven `beads-*` hook files exist on disk and in git but are **not** in the registry. However `cli/src/core/global-hooks-bootstrap.ts:185` copies the **entire** `.xtrm/hooks` directory (`fs.copy(sourceHooksRoot, targetHooksRoot, …)`), and `drift.ts` only validates registry-listed files. So the beads-* files are shipped and globally installed, are invisible to drift detection, and are no longer referenced by the canonical hook template.

Registry-listed (14): `README.md`, `gitnexus/gitnexus-hook.cjs`, `inbox-reminder-stop.mjs`, `quality-check-env.mjs`, `quality-check.cjs`, `quality-check.py`, `specialists-agent-guard.mjs`, `statusline.mjs`, `statusline.test.mjs`, `worktree-boundary.mjs`, `worktree-reap-sweep.mjs`, `xtrm-logger.mjs`, `xtrm-session-logger.mjs`, `xtrm-tool-logger.mjs`.
On disk but not registry-listed (11): `beads-claim-sync.mjs`, `beads-commit-gate.mjs`, `beads-compact-restore.mjs`, `beads-compact-save.mjs`, `beads-edit-gate.mjs`, `beads-gate-core.mjs`, `beads-gate-messages.mjs`, `beads-gate-utils.mjs`, `beads-status-cache.mjs`, `beads-status-cache.test.mjs`, `beads-stop-gate.mjs`.

| Current surface | Current behavior | Current owner | Target owner | Target primitive | Evidence | Action | Upstream dependency | Ordering | Acceptance proof | Migration hazard | Release impact | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `.xtrm/hooks/beads-edit-gate.mjs` | Claude `PreToolUse Edit\|Write` gate: blocks edits with no `bd kv` claim; also enforces the worktree boundary before the claim check | Core (Beads) | Substrate | `integrations/claude-code/edit-gate.ts` (`evaluateEditState`, `handleEditGate`) — explicitly `// Replaces beads-edit-gate.mjs` | `.xtrm/hooks/beads-edit-gate.mjs:1-85`; wrapper text at `cli/src/core/claude-runtime-sync.ts:343-349`; upstream `integrations/claude-code/edit-gate.ts:1-5`; upstream `integrations/claude-code/hooks.json` `PreToolUse` | DELETE | Substrate Claude plugin installed (`substrate@substrate`) **and** registered in `~/.claude/settings.json` | **Blocking ordering constraint**: Substrate Claude plugin is NOT installed today and `~/.claude/settings.json` has 0 substrate references — removing this gate first leaves Claude with zero edit enforcement | Claude `PreToolUse` on `Edit` blocks with a Substrate claim reason; `packages/substrate/integrations/claude-code/edit-gate.ts` executes | Live `~/.claude/settings.json` still wires this file (see F-25) while the canonical template no longer does — a stale global install is the actual enforcement path on this machine | MAJOR | VERIFIED |
| `.xtrm/hooks/beads-commit-gate.mjs` | Claude `PreToolUse Bash` gate: blocks `git commit` while a `bd` claim is open | Core (Beads) | Substrate | `integrations/claude-code/commit-check.ts` (`handleCommitCheck`) — `// Replaces beads-commit-gate.mjs`; semantics change: validates context and **nudges**, never blocks (upstream `commit-check.ts:5-11`) | `.xtrm/hooks/beads-commit-gate.mjs:1-74`; upstream `integrations/claude-code/commit-check.ts:1-11` (receipt binds SHA **after** commit) | DELETE | Same as above | Same as above | A commit with an open claim produces a nudge string, not a block; `sb provenance dispatch` guidance appears | Behaviour reversal (block → nudge) must be announced; operators who rely on close-before-commit lose that ordering guarantee | MINOR | VERIFIED |
| `.xtrm/hooks/beads-stop-gate.mjs` | Claude `Stop` gate: blocks stop when a `bd` claim is open or xtrm session state shows unfinished closure phases | Core (Beads) | Substrate | `integrations/claude-code/stop-gate.ts` (`handleStopGate`, five §77 signals) — `// Replaces beads-stop-gate.mjs` | `.xtrm/hooks/beads-stop-gate.mjs:1-52`; upstream `integrations/claude-code/stop-gate.ts:1-20` | DELETE | Same as above | Same as above | Stop emits continuity context (never blocks); a mechanical checkpoint exists when none was written | Stop gate never blocks upstream either (`stop-gate.ts:17-20`), so blast radius is the continuity message, not enforcement | MINOR | VERIFIED |
| `.xtrm/hooks/beads-claim-sync.mjs` | Claude `PostToolUse` hook: on `bd update --claim` writes `bd kv set claimed:<session>` and `.xtrm/statusline-claim[-<worktree>]`; on `bd close` removes it | Core (Beads) | Substrate | session binding inside `toolIssueClaim` (`integrations/pi/handlers.ts:161`) and the Claude `runtime.ts` `discoverIssueRef` contract | `.xtrm/hooks/beads-claim-sync.mjs:1-121`; ADR mapping `cli/src/core/claude-runtime-sync.ts:346-347` ("beads-claim-sync becomes native session/claim binding") | DELETE | Same as above | Same as above | Claim state visible to the Substrate tools; no `.xtrm/statusline-claim*` file written | `statusline.mjs` and `custom-footer` still consume `.xtrm/cache/beads-status.json`; deleting only the sync half leaves the cache to be written by `statusline.mjs`'s own refresh path | MINOR | VERIFIED |
| `.xtrm/hooks/beads-compact-save.mjs`, `beads-compact-restore.mjs` | Claude `PreCompact` save + `SessionStart` restore of `in_progress` Beads state into `.xtrm-session-state.json` | Core (Beads) | Substrate | `integrations/claude-code/precompact.ts` + `postcompact.ts` + `session-start.ts` (Resume Capsule injection on `source == "compact"`) | `.xtrm/hooks/beads-compact-save.mjs:1-…`, `beads-compact-restore.mjs:1-…`; ADR mapping `claude-runtime-sync.ts:348-349` ("beads-compact-save/restore become Journal mechanical checkpoint / Resume Capsule"); upstream `integrations/claude-code/precompact.ts:1-6`, `session-start.ts:1-5` | DELETE | Same as above | Same as above | After compaction a `SessionStart source=compact` hook injects the Resume Capsule as `additionalContext`; a mechanical checkpoint row exists in the Journal | These two files feed the same `.xtrm-session-state.json` that `src/core/session-state.ts` reads (A-11) — remove as one unit | MINOR | VERIFIED |
| `.xtrm/hooks/beads-gate-core.mjs` | Shared decision core (`readHookInput`, `resolveSessionContext`, `resolveClaimAndWorkState`, `decideEditGate`, `decideWorktreeBoundary`, `decideCommitGate`, `decideStopGate`) | Core (Beads) | — | upstream `handlers.ts` `evaluateEditGate` / `evaluateStopContinuity`; `claude-code/edit-gate.ts` `evaluateEditState` | `.xtrm/hooks/beads-gate-core.mjs:1-12` (imports `./beads-gate-utils.mjs`); consumed by the four gate scripts | DELETE | none (do not port to Substrate) | with C-1..C-6 | file absent; no `decide*Gate` symbol remains in Core | Do **not** port this module into Substrate — upstream already has typed equivalents; porting would create a third authority | PATCH | VERIFIED |
| `.xtrm/hooks/beads-gate-messages.mjs`, `beads-gate-utils.mjs` | Block/fallback message templates; `withSafeBdContext`, `resolveCwd`, `resolveSessionId`, `readHookInput` helpers | Core (Beads) | — | upstream `handlers.ts` reason strings; `integrations/claude-code/runtime.ts` discovery | `.xtrm/hooks/beads-gate-utils.mjs:1-9`; `.xtrm/hooks/beads-gate-messages.mjs:1-3` | DELETE | none | after C-10 and C-14 re-point | files absent; `xtrm-session-logger.mjs` and `xtrm-tool-logger.mjs` no longer import them | **Load-bearing for two registry-listed hooks** (`xtrm-session-logger.mjs:7`, `xtrm-tool-logger.mjs:8`) — deleting first breaks both | MAJOR | VERIFIED |
| `.xtrm/hooks/beads-status-cache.mjs` | Repo-scoped shared Beads status cache: `runFast`, `resolveMainRoot`, `readCache`, `isFresh`, `writeCache`, `takeLease`/`releaseLease`, `fetchCompact`, `cacheAge` | Core (Beads projection) | Substrate-fed Core cache | `sb issue list/state --project <id> --json` (`packages/substrate/cli/sb.ts:795,804`) | `.xtrm/hooks/beads-status-cache.mjs:1-…`; consumed by `.xtrm/hooks/statusline.mjs:17` and `packages/pi-extensions/extensions/custom-footer/index.ts:34,136-160` | REPLACE | `sb` CLI verbs exist | **Last**: after A-5 and C-15 re-point | no `.xtrm/cache/beads-status.json` written; statusline and footer read an `sb`-backed cache | Even though it is **not** in `registry.json`, it is (a) copied globally by the whole-directory bootstrap, (b) imported by the registry-listed `statusline.mjs`, and (c) imported by the active Core Pi footer. It is currently the single most load-bearing dead-looking file in the hook payload | MAJOR | VERIFIED |
| `.xtrm/hooks/beads-status-cache.test.mjs` | `node:test` suite for the cache module | Core | — | — | file present; not registry-listed | DELETE | none | after C-9 | `npm run test:scripts` / node test run excludes it and stays green | removing the module without the test leaves a dangling require path | PATCH | VERIFIED |
| `.xtrm/hooks/xtrm-session-logger.mjs`, `.xtrm/hooks/xtrm-tool-logger.mjs` | Structured debug events for session start / tool calls; both import `resolveCwd`/`resolveSessionId` from `beads-gate-utils.mjs` | Core | Core (de-Beaded) | local helpers or upstream `runtime.ts` discovery shape | `.xtrm/hooks/xtrm-session-logger.mjs:6-7`; `.xtrm/hooks/xtrm-tool-logger.mjs:7-8` | ADAPT | none | **before** C-8 | both hooks log successfully with no Beads import; `~/.xtrm/logs/skills-migration.jsonl` unchanged in shape | Hard ordering dependency: these are registry-listed and globally installed, and they import a non-registry file | MAJOR | VERIFIED |
| `.xtrm/hooks/statusline.mjs` | Claude `statusLine`: renders git + Beads counts using a detached, lease-protected refresh importing `./beads-status-cache.mjs` | Core façade | Core façade over Substrate projection | `sb issue state --project <id> --json` | `.xtrm/hooks/statusline.mjs:1-17`; wired globally (`~/.claude/settings.json` `statusLine.command`) and by `claude-runtime-sync.ts:308-326` (`ensureGlobalStatusLine`) | ADAPT | unlisted module must be replaced, not removed (see C-9) | with A-5 | statusline renders Substrate counts; `/tmp/xtrm-sl-*` caches still bounded | `ensureGlobalStatusLine` hard-writes `~/.claude/settings.json` `statusLine.command` to `~/.xtrm/hooks/statusline.mjs` and returns early if that file is missing — deleting `statusline.mjs` silently drops the statusline configuration rewrite | MINOR | VERIFIED |
| `.xtrm/hooks/specialists-agent-guard.mjs` + `~/.claude/hooks` symlink | Claude `PreToolUse Agent` gate: blocks the raw `Agent` tool only when a `using-specialists` marker is present in the prompt/transcript tail | Core (Specialists routing) | Specialists (see U-2) or Core | Specialists plugin ships `plugins/specialists/hooks/hooks.json` with `PreToolUse Write\|Edit` → `lease-warn.mjs` only; **no `Agent` matcher** in `/tmp/audit/specialists-master/plugins/specialists/hooks/hooks.json:41-53` | `.xtrm/hooks/specialists-agent-guard.mjs:1-76`; `policies/specialists-agent-guard.json`; upstream plugin hooks file has 4 event keys (`SessionStart`, `PreCompact`, `PostCompact`, `PreToolUse`) with no `Agent` guard | KEEP (pending U-2) | none | none | `Agent` calls still route to `specialists run` when the skill is active | None found upstream to replace it — see U-2 | PATCH | VERIFIED |
| `.xtrm/hooks/inbox-reminder-stop.mjs` | Claude `Stop`: surfaces unread pane-scoped inbound messages once each via the xtmux picker, guarded by `@agent_inbox_reminded_keys` | Core (Channels surface) | Channels / xtmux | xtmux `message-list --pane $TMUX_PANE` (already consumed) | `.xtrm/hooks/inbox-reminder-stop.mjs:1-38`; `policies/inbox-reminder.json` | KEEP | none | none | unread message surfaced once, then silent until ack/reply | none | NONE | VERIFIED |
| `.xtrm/hooks/worktree-boundary.mjs` | Claude `PreToolUse Write\|Edit`: blocks edits outside the active `.xtrm/worktrees/<name>` root | Core | Core (worktree launch) | none upstream | `.xtrm/hooks/worktree-boundary.mjs:1-33`; `policies/worktree-boundary.json`; duplicate inline check at `.xtrm/hooks/beads-edit-gate.mjs:24-43` | KEEP + de-duplicate | none | with C-1 | boundary enforced once, not twice per edit | Two independent implementations of the same boundary exist (`worktree-boundary.mjs` and the inline block in `beads-edit-gate.mjs:24-43`); the inline copy disappears with C-1 | PATCH | VERIFIED |
| `.xtrm/hooks/worktree-reap-sweep.mjs` | Claude `SessionStart`: detached, 6-hour-rate-limited `xt worktree reap --apply --yes` | Core | Core | none upstream | `.xtrm/hooks/worktree-reap-sweep.mjs:1-54`; `policies/worktree-reap.json` | KEEP | none | none | `.xtrm/.last-reap-sweep` stamp written at most once per 6 h | none | NONE | VERIFIED |
| `.xtrm/hooks/quality-check.cjs`, `quality-check.py`, `quality-check-env.mjs` | Post-edit lint/typecheck gates; session-start tool availability probe | Core | Core | none upstream | `.xtrm/hooks/quality-check.cjs` (41 096 bytes), `quality-check.py` (13 403 bytes), `quality-check-env.mjs`; `policies/quality-gates.json`, `quality-gates-env.json` | KEEP | none | none | `tsc`/`eslint`/`ruff` availability reported at session start; post-edit gate output unchanged | none | NONE | VERIFIED |
| `.xtrm/hooks/gitnexus/gitnexus-hook.cjs` | Claude `PostToolUse Bash\|Grep\|Read\|Glob`: `gitnexus augment` enrichment | Core (wiring) / pi-gitnexus (impl) | Core | none | `.xtrm/hooks/gitnexus/gitnexus-hook.cjs`; `policies/gitnexus.json`; `src/manifest.json:71` ("functionality is provided by the npm:pi-gitnexus package") | KEEP | none | none | augmentation still fires; `check:gitnexus-no-counter` green | none | NONE | VERIFIED |
| `.xtrm/hooks/xtrm-logger.mjs` | Structured event logger to `.xtrm/debug.db` | Core | Core | none | `.xtrm/hooks/xtrm-logger.mjs:1-9, 4637 bytes`; imported by 4 hooks | KEEP | none | none | debug events still written | none | NONE | VERIFIED |
| `.xtrm/hooks/README.md` | Documents the Beads gates as current ("Blocks writes/edits without an active issue claim") and points to `../docs/pi-extensions-migration.md` | Core (docs) | — | — | `.xtrm/hooks/README.md` "Beads Issue Tracking Gates" section; it is registry-listed and copied globally | ADAPT | none | with C-1..C-6 | README no longer documents deleted gates | Ships globally as a managed file, so a stale README travels to every machine | PATCH | VERIFIED |
| `hooks` (repo root) | Symlink → `.xtrm/hooks` (11 bytes), so legacy `hooks/...` paths resolve | Core (compat) | Core (compat) | — | `ls -la` shows `hooks -> .xtrm/hooks`; `git ls-files hooks` = 0; `.gitignore:144-145` un-ignores `.xtrm/hooks` only | COMPAT | none | none | documented as a compat alias; nothing in `cli/src` references the bare `hooks/` path | **Not a duplicate** — `diff -rq hooks .xtrm/hooks` exits 0 because it is the same inode tree (INITIAL SUSPICION REFUTED). `git check-ignore` reports "beyond a symbolic link" | NONE | VERIFIED |
| `.xtrm/registry.json` `assets.hooks` vs `global-hooks-bootstrap.ts` | Registry tracks 14 hook files; bootstrap copies the whole directory (25 files) and writes an installer manifest derived from a full source walk (`global-hooks-bootstrap.ts:192`) | Core | Core | registry-driven copy, or explicit dependency declaration | `cli/src/core/global-hooks-bootstrap.ts:185,192-193`; `cli/src/core/drift.ts:47-79` (validates registry entries only); registry `assets.hooks.files` = 14 keys | ADAPT | none | before C-1..C-11 | a file present on disk but absent from the registry either fails drift or is legitimately untracked, by an explicit rule; `computeSourceFingerprint` (`global-hooks-bootstrap.ts:100-134`) hashes all 25 files so a beads-only edit still forces a full re-copy | The registry is not the install filter for hooks, so "not in registry" has so far read as "not shipped" when it means only "not drift-checked" — this is the root cause of the beads-* files surviving three cleanup waves | PATCH | VERIFIED |
| `.claude/hooks/specialists-complete.mjs`, `specialists-memory-cache-sync.mjs`, `specialists-session-start.mjs` | Three **tracked symlinks** (git mode `120000`) pointing to `../../.xtrm/hooks/specialists/<name>.mjs`; the target directory is empty | Core | — | — | `git ls-files -s .claude/hooks/` shows mode `120000` for all three; `ls -la .claude/hooks/` shows symlinks; `ls .xtrm/hooks/specialists/` is empty; deletion commits `75ee080e` (retired dead payloads) and `959c7718` (bd-memory) removed the targets | DELETE | none | none | symlinks absent; `.claude/hooks` no longer contains dangling entries | Dangling since 2026-07-25 (`75ee080e`) — any consumer that resolves them gets ENOENT. `.claude/hooks` is not in `package.json` `files`, so only Core checkouts are affected | PATCH | VERIFIED |
| `.xtrm/hooks/specialists/` | Empty directory left behind by the same deletion | Core | — | — | `ls .xtrm/hooks/specialists/` empty; `git ls-files .xtrm/hooks/specialists` empty (git does not track empty dirs) | DELETE | none | none | directory absent | none | NONE | VERIFIED |

### D. Policy compiler and managed config

| Current surface | Current behavior | Current owner | Target owner | Target primitive | Evidence | Action | Upstream dependency | Ordering | Acceptance proof | Migration hazard | Release impact | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `policies/beads.json` | `description` says "RETIRED (Beads to Substrate migration) … retained so `--check-pi` stays green"; declares `pi.extension = packages/pi-extensions/extensions/beads/index.ts` with events `session_start/tool_call/tool_result` | Core | — | `integrations/pi/extension.ts` | `policies/beads.json:2-4`; `scripts/compile-policies.mjs:207-210` (`--check-pi` expects only policies declaring `pi.extension`) | DELETE | none | **After** the upstream Pi stop/continuity wire lands (A-3) and after `--check-pi` is repointed | `policies/` contains no Beads policy; `--check-pi` green | This file is the **only** reason `beads` is still declared a policy-managed Pi extension; removing it simultaneously removes the manifest/registry justification | PATCH | VERIFIED |
| `policies/session-flow.json` | Same pattern: RETIRED, declares `pi.extension` with events `tool_result/agent_end` | Core | — | upstream `evaluateStopContinuity` | `policies/session-flow.json:2-4` | DELETE | same | same | as above | as above | PATCH | VERIFIED |
| `policies/quality-gates.json` | `runtime: "both"`, declares BOTH Claude hooks and `pi.extension = .../quality-gates/index.ts` | Core | Core | `.xtrm/hooks/quality-check.*` | `policies/quality-gates.json`; `src/manifest.json:67` marks `quality-gates` **disabled** ("Hook-script lookup still targets .claude/hooks") | ADAPT | none | none | policy and manifest agree on quality-gates | Two Core config surfaces disagree (policy says deploy, manifest says disabled) — same class of defect as A-13 | PATCH | VERIFIED |
| `scripts/compile-policies.mjs --check-pi` | Compares policy-declared extension names against loose directories in `${PI_AGENT_DIR}/extensions`. Run locally it **fails**: "Expected 3, Deployed 0". CI fabricates the directory (`ci.yml:73-77`: `mkdir` + `cp -r packages/pi-extensions/extensions/*`), and the unit test does the same into a tmpdir (`cli/src/tests/policy-parity.test.ts:189-206`) | Core (stale model) | Core | `cli/src/core/pi-runtime.ts` package-based install (`PI_MIRROR_ROOTS` at `pi-runtime.ts:29-36`; `PROJECT_EXTENSION_PACKAGE_ID = 'npm:@jaggerxtrm/pi-extensions'` at `:193`) | executed `node scripts/compile-policies.mjs --check-pi` → exit non-zero, `~/.pi/agent/extensions` contains 0 of `beads`/`quality-gates`/`session-flow`; `~/.pi/agent/settings.json` `packages[]` instead points at `/home/dawid/dev/core/packages/pi-extensions` | ADAPT | none | independent | `--check-pi` validates the real deployed set (package name + activated manifest ids), not a fabricated copy | False-green in CI by construction; the check can never detect a real deployment drift. RECOMMENDATION: derive the check from the active manifest + the Pi settings `packages[]` entry | PATCH | VERIFIED |
| `.xtrm/config/hooks.json` (canonical template) | Wires: SessionStart `quality-check-env`/`xtrm-session-logger`/`worktree-reap-sweep`; PreToolUse `worktree-boundary` (write tools) + `specialists-agent-guard` (Agent); PostToolUse `quality-check.cjs` + `quality-check.py` (write tools), `gitnexus-hook.cjs`, `xtrm-tool-logger`; Stop `inbox-reminder-stop`. **No beads-* entries** | Core | Core | — | `.xtrm/config/hooks.json:1-…`; read in full | KEEP | none | none | `npm run check-policies` green | This file is already migrated; the stale global `~/.xtrm/config/hooks.json` (F-25) is the divergence | NONE | VERIFIED |
| `.xtrm/config/instructions/{agent-contract,agents-top,claude-top,global-system-prompt}.md` | Managed global-prompt blocks; already Substrate-first ("discover the ready Issue revision … claim it (`sb issue claim`)"; "Substrate owns durable work; Git owns code truth"); `global-system-prompt.md` carries the python/tool doctrine | Core | Core | none upstream | `.xtrm/config/instructions/agents-top.md:1-40`; `global-prompt-sync.ts:1-39` (marker contract `<!-- xtrm:global-prompt:start -->`) | KEEP | none | none | prompt markers intact; bytes outside the pair preserved | Docs are target-state while runtime is legacy (F-25) — the divergence is the deliverable risk, not these files | NONE | VERIFIED |
| `.xtrm/config/settings.json` | Claude permission allowlist includes `Bash(bd show:*)`, `Bash(bd list:*)`, `Bash(bd ready:*)`, `Bash(bd stats:*)`, `Bash(bd search:*)` | Core | Core | `sb` verbs | `.xtrm/config/settings.json` `permissions.allow`; registry-listed as a `config` asset (16 files) | ADAPT | none | none | allowlist references `sb issue …` not `bd` | Allowlisting `bd` keeps the retired tracker first-class in the permission surface | PATCH | VERIFIED |
| `policies/xtrm-debug-logger.json`, `gitnexus.json`, `inbox-reminder.json`, `quality-gates-env.json`, `worktree-boundary.json`, `worktree-reap.json`, `specialists-agent-guard.json` | Non-Beads policy declarations matching live hooks | Core | Core | none | `policies/*.json` read in full | KEEP | none | none | `--check` green | none | NONE | VERIFIED |
| `.xtrm/session-meta.json` | `{runtime: "pi", launchedAt: …}` | Core | Core | none | `.xtrm/session-meta.json`; referenced by `cli/src/tests/worktree-session-launch.test.ts:80` (plants `<worktree>/.xtrm/hooks/statusline.mjs`) | KEEP | none | none | meta file written on launch | none | NONE | VERIFIED |
| `.xtrm/specialists-source.json` | Pins Specialists `config/skills` at `5d2f2907…` with per-file git blob SHAs for `using-specialists` and `update-specialists` | Core (vendoring) / Specialists (content) | Core (vendoring) | Specialists repo as source of truth | `.xtrm/specialists-source.json`; `scripts/{vendor-specialists-from-manifest,vendor-specialists-skills,verify-specialists-vendor,check-vendored-specialists-skill-parity}.mjs`; `npm run check:specialists-vendor` | KEEP | upstream tag/ref to pin | none | `check:vendored-specialists-parity` green after re-vendor | none | NONE | VERIFIED |

### E. Claude/Pi wiring and live global state

| Current surface | Current behavior | Current owner | Target owner | Target primitive | Evidence | Action | Upstream dependency | Ordering | Acceptance proof | Migration hazard | Release impact | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `cli/src/core/claude-runtime-sync.ts` | Generates Claude hook settings from `.xtrm/config/hooks.json`, rewrites `${CLAUDE_PLUGIN_ROOT}/hooks/...` to the resolved hook dir, tags wrappers `_source: xtrm-global` + `_xtrm.hash`, merges ownership-safely, and writes `statusLine` | Core | Core (installation façade) | template is the only authority | `claude-runtime-sync.ts:1-60, 120-200, 308-326, 333-...`; ADR disposition comment at `:343-350`; `ensureGlobalStatusLine` at `:308-326` | KEEP | none | none | `reconcileGlobalClaudeHooks` output matches the canonical template | Its behaviour is template-driven, so it will faithfully install whatever the template says — including a re-introduced `beads-*` wiring | NONE | VERIFIED |
| `cli/src/core/pi-runtime-hooks.ts` | Same reconcile for `~/.pi/agent/settings.json` Pi hooks | Core | Core | template | `pi-runtime-hooks.ts:1-66`; SEAM comment at `:8-11` ("Pi hook successors for the retired beads-* gates are owned by the hook-successor bead. This reconciler stays generic — it merges whatever the canonical template declares, which no longer includes beads-* hooks") | KEEP | none | none | Pi hook settings converge on the template | none | NONE | VERIFIED |
| `cli/src/core/global-hooks-bootstrap.ts` | Whole-directory copy of `.xtrm/hooks` → `~/.xtrm/hooks`, plus whole-file copy of `.xtrm/config/hooks.json`, installer manifest, and a source fingerprint over **all** files | Core | Core | registry-filtered copy *or* explicit dependency declaration | `global-hooks-bootstrap.ts:100-134` (fingerprint), `:179-198` (copy + manifest), `:42` (`COPY_FILTER`) | ADAPT | none | before C-1..C-11 | `~/.xtrm/hooks` contains exactly the intended set; drift detection covers every installed hook | Cause of the "dead but installed" class: 11 bead files are installed on every machine yet only 14 of 25 are drift-checked | PATCH | VERIFIED |
| `~/.claude/settings.json` (live) | Wires beads gates that the canonical template no longer declares: SessionStart `project-memory.mjs`, `beads-compact-restore.mjs`; PreToolUse write tools `beads-edit-gate.mjs`; PreToolUse `Bash` `beads-commit-gate.mjs`; PostToolUse shell `beads-claim-sync.mjs`; Stop `beads-stop-gate.mjs`, `beads-memory-gate.mjs`. `project-memory.mjs` and `beads-memory-gate.mjs` were **deleted** in `959c7718` | Core (stale global state) | Substrate | `substrate@substrate` Claude plugin hooks (`integrations/claude-code/hooks.json`) | `python3` parse of `~/.claude/settings.json`; file list confirmed against `~/.xtrm/hooks/` (which still carries `beads-memory-gate.mjs` from a pre-`959c7718` install) and `git show --stat 959c7718` (deletes `beads-memory-gate.mjs`, `project-memory.mjs`) | REPLACE | Substrate Claude plugin install + `claude plugin install substrate@substrate -s user -y` (`integrations/setup.ts:288`) | **Blocking**: enroll Substrate first; then run `xt install` to reconcile; then remove the beads files | `~/.claude/settings.json` Stop hooks no longer reference a deleted path; every gate resolves to an existing file; the substrate plugin is present in `installed_plugins.json` | **Highest-severity environment finding**: live enforcement on this machine runs Core's retired Beads gates, two of which point at deleted files, and no Substrate enforcement exists at all. INFERENCE: `xt install` has not been run from a post-`959c7718` revision (global `~/.xtrm/hooks/state.json` `installedFrom: /home/dawid/dev/core` with a pre-`#639` fingerprint) | MAJOR | VERIFIED (files) / INFERRED (causality) |
| `~/.pi/agent/settings.json` (live) `hooks` | SessionStart block includes `project-memory.mjs` (deleted) and `beads-compact-restore.mjs`; PreToolUse write tools include `beads-edit-gate.mjs` | Core (stale global state) | Substrate / none | `integrations/pi/extension.ts` `tool_call` gate | `python3` parse of `~/.pi/agent/settings.json` `hooks` | REPLACE | Substrate Pi package enrolled | after Claude ordering step | Pi settings `hooks` contain only template-derived entries; `@jaggerxtrm/substrate` appears in `packages[]` | Same root cause as F-25 | MINOR | VERIFIED |
| Substrate enrollment (both surfaces) | `installed_plugins.json` has no `substrate@substrate`; `known_marketplaces.json` still lists `beads-marketplace`; `~/.pi/agent/settings.json` `packages[]` has no substrate entry | — | Substrate | `integrations/setup.ts` `planCommands` (`:252-262`) and `checkEnrollment` | `python3` parse of `~/.claude/plugins/{installed_plugins,known_marketplaces}.json`, `~/.pi/agent/settings.json`; upstream `setup.ts:1-30, 158-200, 252-320` | REPLACE | none | **First** — every gate replacement depends on it | `node integrations/setup.ts check --json` returns `ok: true` with all six `EXPECTED_ENROLLMENT_ITEMS` (`setup.ts:281-288`), including `beads-absent` | `beads-marketplace` lingering is explicitly one of the remnants `probeInstalledPlugins` reports (`setup.ts:150-175`) | MAJOR | VERIFIED |
| Core git hooks: `.githooks/{prepare-commit-msg,pre-commit,pre-push}` + `*.bd-sync` + `board-audit-*` | Beads-managed blocks (`bd hooks run prepare-commit-msg`, `bd hooks run pre-commit`) plus a custom `.beads/issues.jsonl` auto-stage block | Core (Beads) | Git provenance via Substrate | `packages/substrate/hooks/{prepare-commit-msg.ts,post-commit.ts,post-rewrite.ts}` | `.githooks/prepare-commit-msg:1-55` (`--- BEGIN BEADS INTEGRATION v1.2.2 ---`), `.githooks/pre-commit:1-75`; upstream `packages/substrate/hooks/prepare-commit-msg.ts:1-39`, `post-commit.ts`, `post-rewrite.ts`; upstream `src/domain/provenance.ts:89` ("Host-owned trailer block appended by prepare-commit-msg") | REPLACE | **Missing upstream primitive**: `integrations/setup.ts` `planCommands` (`:252-262`) has no step that installs `packages/substrate/hooks/*` into a repo, and no `core.hooksPath` handling exists anywhere under `/tmp/audit/xtrm-main/packages/substrate/` (grep for `core.hooksPath` returns nothing) | after enrollment; upstream must first ship an install step | `git config core.hooksPath` points at installed Substrate hooks; a commit carries XTRM trailers; `post-commit` binds the receipt SHA | Without an upstream installer, replacing Core's Beads git hooks leaves provenance trailers unwritten, and `commit-check.ts:64` guidance becomes unactionable | MAJOR | VERIFIED (Core + upstream absence) |
| `cli/src/core/bd-auto-stage-patch.ts` | Flips `.beads/config.yaml export.git-add=false` and injects a Beads auto-stage block into the configured `core.hooksPath` `pre-commit` | Core (Beads) | — | — | `bd-auto-stage-patch.ts:1-70` | DELETE | none | with git-hook replacement | `.beads` absent; no `XTRM bd auto-stage snapshot` block in any hook | Beads-only mechanism (JSONL snapshot staging) with no Substrate analogue needed | PATCH | VERIFIED |
| `cli/src/core/beads-shared-server.ts` | Flips `.beads/config.yaml dolt.shared-server=true` | Core (Beads) | — | — | `beads-shared-server.ts:1-32`; called from `cli/src/commands/install.ts:249` (`ensureBeadsSharedServerEnabled`) | DELETE | none | with `.beads` retirement | no `.beads` config written by install | Called unconditionally on every `xt install` (guarded only by `.beads` presence) | PATCH | VERIFIED |
| `cli/src/core/plugin-era-cleanup.ts` | `XTRM_MANAGED_PI_EXTENSIONS` includes `beads`, `session-flow`, `quality-gates`, `custom-footer`, `service-skills`, `xtrm-loader` for **on-disk cleanup of stale global installs**, with the comment "Retired extension ids stay in this set so stale global installs are still cleaned: membership drives on-disk cleanup, not registration" | Core | Core | none | `plugin-era-cleanup.ts:60-90`; `cli/src/core/pi-runtime.ts:280-286` (`~/.pi/agent/extensions/` cleanup) | KEEP | none | none | `--prune` removes loose-dir installs without touching the active manifest package | The set's membership deliberately overlaps the active manifest (`beads`, `session-flow`, `custom-footer` are active+required). INFERENCE: the overlap is intentional (package install vs loose-dir cleanup) but it makes "is this extension shipped?" unanswerable from this file | PATCH | VERIFIED |
| `cli/src/core/substrate.ts` | Consumer boundary: `sb --version`, `sb doctor --json`, `sb project create/link`, `setup.ts check/plan --json`, whole-plan validation before execution, per-command timeouts | Core | Core | upstream `integrations/setup.ts` contract | `substrate.ts:1-41` (contract docs), `:281-288` (`EXPECTED_ENROLLMENT_ITEMS`), `:414-449` (`runSetupCheck`), `:484-564` (`validatePlanCommands` + `executePlanCommands`) | KEEP | upstream contract stability | none | `xt init`/`xt doctor` consume the six-item enrollment contract | Lane 2 documents that the consumed `sb` surface is pinned to 0.1.x — that is a Lane 2 finding, not a runtime-enforcement one | NONE | VERIFIED |
| `cli/src/core/substrate-migration.ts` | A8 detection only: `.beads` present → fail-closed abort of `xt update --apply`; honors `.substrate-migrated.json`; performs no import, backup, receipt, or cleanup | Core | Core | upstream `sb import beads --file … --json` (A9) | `substrate-migration.ts:1-98`; `MIGRATION_MARKER` at `:12`; `migrationBlockedReason` at `:42-55` | KEEP | A9 pipeline | none | `xt update --apply` on a `.beads` repo aborts with the documented remediation and zero mutations | none | NONE | VERIFIED |
| `cli/src/core/init-verification.ts` | Project bootstrap gate is **Substrate-first**: `substrateReady` = `sb --version` + state.db + linked project + enrollment health, replacing the old `.beads`-directory gate | Core | Core | — | `init-verification.ts:190-217` ("Substrate-first gate (replaces the old `.beads`-directory gate)"), `:324` | KEEP | none | none | `xt doctor` reports `substrateReady` independently of `.beads` | Confirms Core's *verification* layer already migrated while the *enforcement* layer (A, C, F) did not — the core asymmetry of this lane | NONE | VERIFIED |
| `cli/src/core/pi-runtime.ts` | Unified Pi runtime: mirrors extension sources (`PI_MIRROR_ROOTS`), repairs `@xtrm/pi-core` symlinks, manages `settings.packages`, removes legacy `.pi/extensions` copies | Core | Core | none upstream | `pi-runtime.ts:29-36, 120, 183-202, 884-1030, 1157-1226` | KEEP | none | none | `xt install` resolves Pi packages without touching the active manifest | none | NONE | VERIFIED |

### F. Non-Substrate enforcement domains (for completeness)

| Current surface | Current behavior | Current owner | Target owner | Target primitive | Evidence | Action | Upstream dependency | Ordering | Acceptance proof | Migration hazard | Release impact | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `.claude/git-hooks/doc_reminder.py`, `.claude/git-hooks/skill_staleness.py` | Git-hook doc reminder and skill staleness checks; tracked in Core | Core | Core | none | `.claude/git-hooks/*` (tracked: `git ls-files .claude/` lists both) | KEEP | none | none | hooks install and run | none | NONE | VERIFIED |
| `.claude/rules/openwolf.md`, `.claude/docs/*-readme.md` | Claude rules/docs payload | Core | Core | none | `git ls-files .claude/` | KEEP | none | none | unchanged | none | NONE | VERIFIED |
| Specialists Claude plugin (`plugins/specialists/hooks/hooks.json`) | Upstream Specialists wires SessionStart `session-start.mjs` + `wake-watch.mjs` (`asyncRewake`, 900 s), PreCompact/PostCompact, PreToolUse write tools `lease-warn.mjs`, MCP server `.mcp.json` | Specialists | Specialists | — | `/tmp/audit/specialists-master/plugins/specialists/hooks/hooks.json:1-57`; `.claude-plugin/plugin.json` ("Consumes the Substrate work authority; it is not Substrate"); `~/.claude/plugins/installed_plugins.json` shows `specialists@xtrm` installed | KEEP | none | none | Specialists plugin hooks fire; `xtrm marketplace` points at `/home/dawid/dev/specialists` | Core's `.xtrm/hooks/specialists-agent-guard.mjs` is a Core-side routing guard with **no Specialists-side equivalent** (the plugin has no `Agent` matcher) — see U-2 | NONE | VERIFIED |
| `~/.claude/hooks` symlinks to `.xtrm/hooks/specialists/*` (tracked in Core) | See C-22 | Core | — | — | as C-22 | DELETE | none | none | as C-22 | Covered under C-22; listed here because the target payload belongs to Specialists | PATCH | VERIFIED |

---

## Duplicated authority — same gate implemented in both Core and Substrate

Every row below is the same gate implemented twice, with the upstream file explicitly naming itself as the replacement.

| # | Gate | Core implementation (live on this machine) | Substrate implementation (not enrolled) | Upstream replacement statement |
|---|---|---|---|---|
| 1 | Edit/write gate (Claude) | `.xtrm/hooks/beads-edit-gate.mjs` | `/tmp/audit/xtrm-main/packages/substrate/integrations/claude-code/edit-gate.ts` | `edit-gate.ts:1` — `// Substrate edit gate (§38, bead xtrm-6qu.6). Replaces beads-edit-gate.mjs.` |
| 2 | Commit gate (Claude) | `.xtrm/hooks/beads-commit-gate.mjs` | `.../integrations/claude-code/commit-check.ts` | `commit-check.ts:1` — `// Substrate commit check (§38 …). Replaces beads-commit-gate.mjs.` |
| 3 | Stop/continuity gate (Claude) | `.xtrm/hooks/beads-stop-gate.mjs` | `.../integrations/claude-code/stop-gate.ts` + `integrations/pi/handlers.ts:390 evaluateStopContinuity` | `stop-gate.ts:1` — `// Substrate stop gate (H1 / ADR §§76-77, §38, bead xtrm-6qu.6). Replaces beads-stop-gate.mjs.` |
| 4 | Edit/write gate (Pi) | `packages/pi-extensions/extensions/beads/index.ts:88-129` | `integrations/pi/extension.ts:183-192` → `integrations/pi/handlers.ts:332 decidePiEditCall` | `handlers.ts:293-298` — `// gates ported from Beads (§§38-39, bead xtrm-6qu.6) … routes edit/write calls through decidePiEditCall` |
| 5 | Claim recording / session binding | `.xtrm/hooks/beads-claim-sync.mjs` (Claude) + `extensions/beads/index.ts:144-154` (Pi) | `integrations/pi/handlers.ts:151-163 toolIssueClaim` + `SessionBindings`; Claude `runtime.ts:53-66 discoverIssueRef` | `claude-runtime-sync.ts:346` — `beads-claim-sync becomes native session/claim binding` |
| 6 | Compaction save (Claude) | `.xtrm/hooks/beads-compact-save.mjs` | `.../integrations/claude-code/precompact.ts` | `claude-runtime-sync.ts:348-349` — `beads-compact-save/restore become Journal mechanical checkpoint / Resume Capsule` |
| 7 | Compaction restore / post-compact resume | `.xtrm/hooks/beads-compact-restore.mjs` | `.../integrations/claude-code/session-start.ts` + `postcompact.ts` | same as row 6 |
| 8 | Pi compaction checkpoint | none in Core | `integrations/pi/extension.ts:210-263` `session_before_compact` → `handlers.ts:259 runBeforeCompact` | n/a — Core has no Pi equivalent; Substrate is the sole authority |
| 9 | Claim/continuity stop gate (Pi) | `extensions/session-flow/index.ts:67-95` (non-blocking nudge) | `integrations/pi/handlers.ts:390 evaluateStopContinuity` — **exported but not wired** in `integrations/pi/extension.ts` | `handlers.ts:373-375` — `// H1 §77 five-signal input` (no Pi `agent_end` registration exists) |
| 10 | Work-state projection for status display | `.xtrm/hooks/beads-status-cache.mjs` (+ `.xtrm/hooks/statusline.mjs`, `extensions/custom-footer/index.ts`) | `sb issue list/state --project <id> --json` (`cli/sb.ts:795,804`); `integrations/pi/handlers.ts:124 toolIssueGet` exposes readiness/claim | `claude-runtime-sync.ts:349` — `beads-status-cache becomes a Substrate projection or is retired` |
| 11 | Worktree boundary | `.xtrm/hooks/worktree-boundary.mjs` **and** inline at `.xtrm/hooks/beads-edit-gate.mjs:24-43` | none (Core domain) | n/a — but row 11 is a Core-internal duplication, not Core↔Substrate |
| 12 | Git provenance / claim-before-commit | `.githooks/*` Beads-managed blocks + `cli/src/core/bd-auto-stage-patch.ts` | `packages/substrate/hooks/{prepare-commit-msg,post-commit,post-rewrite}.ts` + `provenance-service.ts` | `commit-check.ts:5-11` — `Existing Substrate Git provenance owns trailers and SHA binding (prepare-commit-msg, post-commit hooks)` |
| 13 | Advertised workflow authority in the injected contract | `.xtrm/config/instructions/*` already say `sb issue claim` / `sb issue resume` | — | The prompt/docs are migrated; rows 1-12 are the runtime that disagrees with them |

---

## Top 10 highest-risk duplicated-authority items, with the exact replacing upstream primitive

| Rank | Item | Risk | Exact upstream primitive that replaces it |
|---|---|---|---|
| 1 | Live `~/.claude/settings.json` + `~/.pi/agent/settings.json` still wire Core's Beads gates while two of the referenced files (`beads-memory-gate.mjs`, `project-memory.mjs`) were deleted in `959c7718` | Enforcement silently no-ops or errors on every Stop; Substrate is not installed, so the machine has **no** working claim enforcement on either surface | `python3 /tmp/audit/xtrm-main/packages/substrate/integrations/setup.ts check --json` (six-item enrollment contract, `setup.ts:281-288`) + `substrate@substrate` plugin hooks (`integrations/claude-code/hooks.json`) + `pi install <substrate-dir>` (`setup.ts:252-262`) |
| 2 | `.xtrm/hooks/beads-edit-gate.mjs` vs `integrations/claude-code/edit-gate.ts` | Same gate, two authorities; the Core copy blocks, the Substrate copy blocks with holder+activation semantics and a workspace write lease | `integrations/claude-code/edit-gate.ts` → `evaluateEditState()`; Pi counterpart `integrations/pi/handlers.ts:332 decidePiEditCall()` |
| 3 | `packages/pi-extensions/extensions/beads/index.ts` edit gate vs `integrations/pi/extension.ts:183-192` | Pi edits are gated by an 850 ms-per-call `bd kv` subprocess chain; the gate is **already inert in worktrees** because `isBeadsProject` requires `.beads` in `cwd` (`src/core/adapter.ts:39-41` verified against this worktree) | `integrations/pi/extension.ts:183-192` `pi.on("tool_call")` → `handlers.ts:332` |
| 4 | `.xtrm/hooks/beads-status-cache.mjs` — not in `registry.json`, but globally installed **and** imported by registry-listed `statusline.mjs:17` and active `custom-footer/index.ts:34` | Deleting the "dead" file breaks two live surfaces; keeping it keeps a Beads-shaped projection as the only work-state display | `sb issue state --project <id> --json` / `sb issue list --project <id> --json` (`packages/substrate/cli/sb.ts:795,804`), consumed by a Core-owned cache |
| 5 | `.xtrm/hooks/beads-gate-utils.mjs` — not in `registry.json`, but imported by registry-listed `xtrm-session-logger.mjs:7` and `xtrm-tool-logger.mjs:8` | Same class as #4: invisible to `drift.ts`, load-bearing at runtime | Core-local helpers; upstream equivalent shape in `integrations/claude-code/runtime.ts:30-66` (`resolveDbPath` / `discoverIssueRef`) |
| 6 | `.xtrm/hooks/beads-stop-gate.mjs` vs `integrations/claude-code/stop-gate.ts` (+ unwired Pi counterpart) | Two stop semantics: Core blocks, Substrate emits continuity and never blocks; Core's version reads `.xtrm-session-state.json` written by files retired in the same wave | `integrations/claude-code/stop-gate.ts handleStopGate()`; Pi side requires wiring `handlers.ts:390 evaluateStopContinuity` into `integrations/pi/extension.ts` |
| 7 | `.xtrm/hooks/beads-commit-gate.mjs` (blocks close-first) vs `integrations/claude-code/commit-check.ts` (nudges, recovers via post-commit bind) | Behaviour reversal; Core's ordering guarantee (close before commit) is the opposite of Substrate's provenance flow | `integrations/claude-code/commit-check.ts handleCommitCheck()` + `packages/substrate/hooks/post-commit.ts` |
| 8 | `.githooks/*` Beads blocks + `bd-auto-stage-patch.ts` vs Substrate git provenance hooks | Core manages `core.hooksPath` and `.beads` config; Substrate owns trailers and SHA binding. **No upstream installer exists** (`setup.ts planCommands` has no git-hook step; no `core.hooksPath` handling anywhere in the Substrate package) | `packages/substrate/hooks/{prepare-commit-msg.ts,post-commit.ts,post-rewrite.ts}` — and a new `planCommands` entry in `integrations/setup.ts` to install them |
| 9 | `.xtrm/hooks/beads-compact-save.mjs` / `beads-compact-restore.mjs` vs `integrations/claude-code/{precompact,postcompact,session-start}.ts` | Two compaction state stores; the Core pair writes/reads `.xtrm-session-state.json`, whose only reader (`src/core/session-state.ts`) has zero callers | `integrations/claude-code/precompact.ts handlePreCompact()` + `session-start.ts handleSessionStart()`; Pi side `integrations/pi/extension.ts:210-263` + `handlers.ts:259 runBeforeCompact` |
| 10 | `policies/beads.json` + `policies/session-flow.json` declaring retired extensions RETIRED while `src/manifest.json` marks them `required: true` and `--check-pi` is a fabricated-dir check (`ci.yml:73-77`) | The two Core-side declaration systems cannot detect the contradiction, and the check that should catch it is green by construction | One authority: `packages/pi-extensions/src/manifest.json` (active set) validated against the real Pi `settings.packages[]` entry and the real `node_modules/@jaggerxtrm/pi-extensions` install, replacing `scripts/compile-policies.mjs` `runPiCheck` (`compile-policies.mjs:189-243`) |

---

## UNRESOLVED

| ID | Question | Why it blocks | Owner who must answer |
|---|---|---|---|
| U-1 | Who is the target owner of Core's Pi-side stop/continuity nudge (`extensions/session-flow/index.ts:67-95`) — Substrate, or is it dropped entirely? Upstream exports `evaluateStopContinuity` (`integrations/pi/handlers.ts:390`) but never registers it in `integrations/pi/extension.ts`, so Substrate Pi today has no stop behaviour at all. | Cannot classify A-3 as DELETE vs ADAPT, and cannot order the removal safely. If the answer is "Substrate should wire it", the upstream change is a one-line `pi.on("agent_end")` in `integrations/pi/extension.ts`. | Substrate maintainer (`xtrm-main` Pi integration owner) |
| U-2 | Does Specialists own the raw-`Agent`-tool guard, and if so where is the Specialists-side primitive? Core ships `.xtrm/hooks/specialists-agent-guard.mjs` (active policy `policies/specialists-agent-guard.json`), but the Specialists plugin (`plugins/specialists/hooks/hooks.json`) has no `Agent` matcher and no equivalent guard. | Cannot decide KEEP vs MOVE for C-16. A Core-side guard on another product's tool routing is exactly the duplicate-authority shape this audit is looking for. | Specialists maintainer |
| U-3 | Which Core revision/machine will perform the gate cutover, and what is the rollback? Live state shows `sb` on PATH and `~/.xtrm/state.db` present, but **no** Substrate enrollment on either runtime, while the beads gates are live and two of the referenced files are already deleted. | The ordering constraint (enroll → verify → reconcile → delete) is a hard prerequisite; without a named cutover owner the next cleanup wave will delete the last working gate. | Core release owner (Lane 3 / migration coordinator) |
| U-4 | Does Substrate intend to ship an installer for `packages/substrate/hooks/{prepare-commit-msg,post-commit,post-rewrite}.ts`? No install step exists in `integrations/setup.ts planCommands` and no `core.hooksPath` handling exists anywhere in the Substrate package. | Blocks F-20 from DELETE→REPLACE; without it, provenance trailers are never written after Core's Beads git hooks are removed. | Substrate maintainer |
| U-5 | Is the tool-name shadowing in `extensions/xtrm-ui/index.ts:1341-1600` (`bash`, `read`, `edit`, `write`, `find`, `grep`, `ls`) an intentional native-tool replacement, and does it survive a Pi tool-registration change? | Determines whether A-9 is KEEP or ADAPT, and whether the Substrate `tool_call` gate keeps seeing `edit`/`write` events if xtrm-ui's registrations change shape. | Core Pi-extensions owner |
| U-6 | Which Core-owned surface is the long-term work-state display authority? `custom-footer` (Pi) and `statusline.mjs` (Claude) both read a Beads cache today, and Substrate exposes `toolIssueGet` / `sb issue state` but no display projection. | Determines whether A-5/C-15 are ADAPT (Core façade over `sb`) or DELETE (Substrate/chain provides the display). | Core UX owner + Substrate maintainer |

---

## Cross-lane seams

- Lane 2 (`lane2-xt-cli-lifecycle.md`) owns `sb` CLI surface drift, package identity, and `xt init` project-identity authority. This lane did not re-derive those; the only overlap is `cli/src/core/substrate.ts`, which both lanes treat as a correctly-shaped consumer boundary.
- No lane covers an **enrollment/cutover plan**; U-3 asks for one. The three findings that make it urgent are F-25 (live stale wiring including deleted files), F-26 (no Substrate enrollment), and A-14 (the Pi gate is already inert in worktrees).
