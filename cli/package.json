{
  "name": "xtrm-cli",
  "private": true,
  "version": "0.12.0",
  "description": "Claude Code tools installer (skills, hooks, MCP servers)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xtrm-dev/core.git"
  },
  "homepage": "https://github.com/xtrm-dev/core#readme",
  "bugs": {
    "url": "https://github.com/xtrm-dev/core/issues"
  },
  "main": "./dist/index.js",
  "type": "module",
  "bin": {
    "xtrm": "dist/index.cjs",
    "xt": "dist/index.cjs",
    "ghgrep": "scripts/ghgrep.mjs"
  },
  "files": [
    "dist",
    "config",
    "scripts/ghgrep.mjs"
  ],
  "scripts": {
    "prebuild": "node -e \"const f=require('fs'),real=p=>{try{return f.lstatSync(p).isDirectory()}catch{return false}};if(process.cwd().includes('/.xtrm/worktrees/')&&!(real('node_modules')&&real('../node_modules'))){console.error('ERROR: refusing to build \u2014 this worktree has no node_modules of its own.\\ntsup would then resolve deps from the main repo and embed worktree-relative paths in cli/dist, so the CI dist check fails.\\nFix: run npm ci at the worktree root AND in cli/, then rebuild.\\nOr build from the main repo: cd <repo-root>/cli && npm run build');process.exit(1)}\"",
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/index.cjs",
    "postbuild": "node -e \"for (const f of ['dist/index.cjs']) { const fs = require('fs'); const lines = fs.readFileSync(f, 'utf8').split('\\n'); fs.writeFileSync(f, lines.map(l => l.replace(/[ \\t]+$/, '')).join('\\n')); }\""
  },
  "dependencies": {
    "boxen": "^8.0.1",
    "cli-table3": "^0.6.5",
    "commander": "^14.0.3",
    "comment-json": "^4.2.3",
    "conf": "^15.1.0",
    "dotenv": "^16.4.5",
    "fs-extra": "^11.2.0",
    "kleur": "^4.1.5",
    "listr2": "^10.1.1",
    "minimist": "^1.2.8",
    "ora": "^9.3.0",
    "project": "^0.1.6",
    "prompts": "^2.4.2",
    "yaml": "^2.4.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^25.3.0",
    "@types/prompts": "^2.4.9",
    "@xtrm/contracts": "^0.11.1",
    "ajv": "^8.20.0",
    "esbuild": "0.28.1",
    "fast-check": "^4.6.0",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "8.0.16",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "overrides": {
    "fast-uri": "^3.1.7",
    "nanoid": "^3.3.18",
    "postcss": "^8.5.23",
    "vite": "8.0.16",
    "esbuild": "0.28.1"
  },
  "pnpm": {
    "overrides": {
      "fast-uri": "^3.1.7",
      "nanoid": "^3.3.18",
      "postcss": "^8.5.23",
      "vite": "8.0.16",
      "esbuild": "0.28.1"
    }
  }
}
