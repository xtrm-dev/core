{
  "$schema": "../vsync_repo/schemas/mcp_servers.schema.json",
  "$metadata": {
    "description": "Core MCP servers - installed by default for all agents",
    "agents": ["claude-code", "gemini", "qwen", "antigravity"],
    "version": "2.1.0"
  },
  "mcpServers": {
    "github-grep": {
      "type": "http",
      "url": "https://mcp.grep.app"
    },
    "deepwiki": {
      "type": "http",
      "url": "https://mcp.deepwiki.com/mcp"
    },
    "exa": {
      "type": "http",
      "url": "https://mcp.exa.ai/mcp?tools=web_search_exa,web_fetch_exa,web_search_advanced_exa,agent_run",
      "headers": { "x-api-key": "${EXA_API_KEY}" },
      "_notes": {
        "description": "Exa semantic web research, source retrieval, advanced search, and authenticated multi-step research",
        "auth": "OAuth preferred; anonymous search/fetch may be rate-limited; never commit the API key"
      }
    },
    "gitnexus": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "gitnexus", "mcp"],
      "env": {},
      "_notes": {
        "description": "Code intelligence MCP - provides graph queries for impact analysis, execution flows, symbol context",
        "prerequisite": "Run 'gitnexus analyze' in the project first to build the index",
        "auto_index": "xtrm project init runs gitnexus analyze automatically"
      }
    }
  }
}
