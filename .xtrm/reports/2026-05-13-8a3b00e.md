---
session_date: 2026-05-13
branch: main
commits: 0
issues_closed: 9
issues_filed: 11
specialist_dispatches: 11
models_used: [openai-codex/gpt-5.3-codex, openai-codex/gpt-5.4-mini]
---

# Session Report — 2026-05-13 (auto-mode stabilization)

## Summary

Second session of the day, run in `using-specialists-auto` mode. Drove the 4-item stabilization audit produced by the morning session (`2026-05-13-0f401e1.md`) to completion: A2 (`xt doctor` cwd), A3 (`xt status` non-interactive), B1 (Pi global root fallback), A1 (install-integration flake), C1 (payload hygiene denylist). Each chain ran executor (or debugger) → reviewer → merge through `sp merge`, then pushed as a feature branch + PR because direct push to `main` is blocked since PR #206. Five PRs queued for review (#224–#228), zero merges to `main` from this session — local main was reset to `origin/main` after each chain's local merge to extract the squashable branch.

Discovered a structural reviewer-visibility bug during the A3 chain: reviewer specialist returned `PARTIAL 91` citing "no gitnexus_impact telemetry" in `sp feed <reviewed_job>`. Verified against `.specialists/db/observability.db` that executor `b385c4` actually made 13 gitnexus tool calls (including 2× impact). Root cause: `sp feed <job-id>` returns only an 8-line tail (last turn's TEXT/TOKNS/TURN+/WAIT events) instead of the full DB-backed replay the `sp feed --help` documents. Filed `unitAI-889dv` (specialists, P1) with repro evidence and two fix paths.

Mid-session, the B1 push was blocked by the pre-push semgrep hook on `cli/src/core/pi-runtime.ts` (4 `path.join(agentDir, ..., npmPackageName, ...)` flagged as path-traversal — `npmPackageName` is from the xt-managed allowlist, not user input). Followed hard rule 8 (no `--no-verify`); operator selected the inline `// nosemgrep` annotation path; dispatched a tiny executor (`xtrm-1hwe`) for the 4-line annotation set, cherry-picked onto the B1 branch. Saved as a precedent memory.

C1's new `check:payload-hygiene` gate immediately surfaced 3 pre-existing packed-artifact leaks (`.xtrm/.../evals/evals.json`, `.xtrm/.../workspace/.../outputs/response.md`, `packages/pi-extensions/.serena/project.yml`). Filed all 3 as P2 `discovered-from:xtrm-7xxz`. Gate works as designed; release blocks until cleanups land.

Session-close included a deep filesystem cleanup: 3 orphan waiting jobs (all from the stalled `xtrm-9xg2.2/3` epic chain) stopped, 52 stale `.worktrees/` dirs and 167 stale `.specialists/jobs/` dirs removed, 1 orphan `dolt sql-server` for `/tmp/bd-proto-test/` killed. Remaining dolt processes (4) all map to legitimate per-project bd state.

## Issues Closed (9)

| ID | Title | Type | Close Reason |
|----|-------|------|--------------|
| `xtrm-sxug` | fix(doctor): resolve project root when xt doctor invoked from subdir | bug | Reviewer PASS first pass. PR #224. |
| `xtrm-dka5` | Review xtrm-sxug doctor cwd fix | task | Reviewer PASS for xtrm-sxug. |
| `xtrm-d3wx` | fix(status): non-interactive mode for xt status | bug | Reviewer PARTIAL 91 invalid (telemetry blind spot — 13 gitnexus calls verified in obs DB). Functional review clean. PR #225. |
| `xtrm-ntf8` | fix(pi-runtime): npm-installed Pi packages reported as installedVersion: null by xt update | bug | Reviewer PARTIAL 88 → PASS after 2 regression tests added (agent-local-wins, scoped-global-not-missing). PR #226. |
| `xtrm-1hwe` | fix(pi-runtime): annotate semgrep false-positives in path.join calls | bug | Operator-approved nosemgrep precedent. PR #226 includes annotations. |
| `xtrm-0kgm` | fix(tests): install-integration flake — hooks.json mutated to non-array by concurrent test | bug | Reviewer PASS 95 first pass. Debugger fixed via `Array.isArray` guard in `resolveHooksForProjectRuntime`. 3-run streak green. PR #227. |
| `xtrm-v03q` | Review xtrm-0kgm install-integration flake fix | task | Reviewer PASS for xtrm-0kgm. |
| `xtrm-7xxz` | Package: add payload hygiene denylist for xtrm-tools npm pack | task | Reviewer PARTIAL 91 → PASS after gate-order fix (absolute-path scan always runs independent of denylist). PR #228. |
| `xtrm-8gy9` | Review xtrm-7xxz payload hygiene denylist | task | Reviewer PASS for xtrm-7xxz. |

## Issues Filed (11)

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| `xtrm-sxug` | P1 | bug | fix(doctor): resolve project root when xt doctor invoked from subdir | A2 from morning stabilization audit. Implementation bead. |
| `xtrm-dka5` | P1 | task | Review xtrm-sxug doctor cwd fix | Reviewer bead. |
| `xtrm-d3wx` | P1 | bug | fix(status): non-interactive mode for xt status | A3 from audit. Implementation bead. |
| `xtrm-2lki` | P1 | task | Review xtrm-d3wx status non-interactive fix | Reviewer bead. |
| `xtrm-1hwe` | P1 | bug | fix(pi-runtime): annotate semgrep false-positives | Surfaced when B1 push blocked by pre-push semgrep hook. Operator-approved nosemgrep precedent. |
| `xtrm-0kgm` | P1 | bug | fix(tests): install-integration flake | A1 from audit. Implementation bead. |
| `xtrm-v03q` | P1 | task | Review xtrm-0kgm install-integration flake fix | Reviewer bead. |
| `xtrm-8gy9` | P1 | task | Review xtrm-7xxz payload hygiene denylist | Reviewer bead for C1. |
| `xtrm-fldz` | P2 | bug | cleanup(pack): remove evals output artifacts from .xtrm/ in pack | discovered-from xtrm-7xxz. Surfaced by new check:payload-hygiene gate. |
| `xtrm-kuss` | P2 | bug | cleanup(pack): remove .xtrm/.../workspace/.../outputs/response.md from pack | discovered-from xtrm-7xxz. |
| `xtrm-of65` | P2 | bug | cleanup(pack): remove packages/pi-extensions/.serena/project.yml from pack | discovered-from xtrm-7xxz. |

Also filed in `dev/specialists`:
| `unitAI-889dv` | P1 | bug | fix(feed): sp feed <job-id> returns partial tail instead of full DB-backed replay | Structural reviewer-visibility bug. Caught when A3 reviewer falsely PARTIAL'd on missing gitnexus telemetry. Two fix paths documented. |

## Specialist Dispatches

| Bead | Job | Role | Outcome | Notes |
|------|-----|------|---------|-------|
| xtrm-sxug | 7dd099 | executor | done | A2 doctor cwd fix |
| xtrm-dka5 | 9b30dd | reviewer | PASS | First-pass clean |
| xtrm-d3wx | b385c4 | executor | done | A3 status non-interactive |
| xtrm-2lki | 397547 | reviewer | PARTIAL→PARTIAL | Telemetry blind spot; operator override after DB verification |
| xtrm-ntf8 | 86dd28 | executor | done (2 turns) | B1 pi global root fallback; PARTIAL fix loop for 2 regression tests |
| xtrm-gmzo | d78a55 | reviewer | PARTIAL→PASS | Re-review after test gaps closed |
| xtrm-1hwe | a9a232 / c1924c | executor | done (2nd run; first cancelled when worktree base needed rebase onto B1 merge) | 4-line nosemgrep annotations |
| xtrm-0kgm | adc54d | debugger | done | A1 install-integration flake; defensive Array.isArray guard |
| xtrm-v03q | 7ef3e3 | reviewer | PASS | First-pass clean, full gitnexus coverage |
| xtrm-7xxz | f7a533 | executor | done (2 turns) | C1 payload hygiene; PARTIAL fix loop for absolute-path scan ordering |
| xtrm-8gy9 | d83531 | reviewer | PARTIAL→PASS | Gate-order issue surfaced and fixed |

Models: `openai-codex/gpt-5.3-codex` (reviewers, debugger), `openai-codex/gpt-5.4-mini` (executors, code-sanity).

## Problems Encountered

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| A3 reviewer returned PARTIAL 91 on "missing gitnexus_impact telemetry" despite executor making 2 impact calls | `sp feed <reviewed_job>` returns 8-line tail, not full DB replay; reviewer's audit path silently misses tool-call evidence. `sp feed --help` claims DB-backed, reality is file-tail. | Verified against `.specialists/db/observability.db` (13 gitnexus calls present). Filed `unitAI-889dv`. Treated PARTIAL as invalid telemetry claim; merged on functional grounds. Saved memory `reviewer-partial-on-missing-gitnexus-impact-telemetry-is`. |
| B1 push blocked by semgrep pre-push hook (8 path-traversal findings) | `cli/src/core/pi-runtime.ts:516,518,595,598` uses `path.join(agentDir, ..., npmPackageName, ...)`. `npmPackageName` is from xt-managed allowlist constants, not user input. Semgrep can't infer the allowlist invariant. | Operator approved inline `// nosemgrep` annotations. Dispatched fresh executor `xtrm-1hwe` for the annotation pass; cherry-picked onto B1 branch. Memory saved as precedent. |
| `sp merge` reported "TypeScript gate failed after merge" but `npm run typecheck --workspace cli` was clean | sp merge invokes tsc with raw CLI args that don't parse correctly; spurious failure. Actual merge always landed. | Verified each time via `npm run typecheck` + `git log` after the gate error. Local merge applied; continued to push. Worth filing if it persists. |
| Executor `xtrm-1hwe` worktree base was `origin/main` (8a3b00e), but the lines it needed to annotate didn't exist there (they're added by the B1 merge at 5f9c096) | Local main had been reset back to origin/main after each PR-branch extraction, so sp's worktree provisioning saw a stale base. | Stopped the first executor, `git rebase 5f9c096` on the executor branch, re-dispatched with `--job <prior>` to reuse the (now rebased) workspace. |
| Workspace had 3 orphan waiting jobs (b4993a, 06e151, 6c1de3) from the stalled `xtrm-9xg2.2/3` epic chain, plus 52 stale `.worktrees/` dirs and 167 stale `.specialists/jobs/` dirs | Accumulated session-end cleanup misses since 2026-05-09. The `xtrm-9xg2` epic chains stalled but their sp-tracked artifacts remained. | At session close: `sp stop` the 3 orphans; `rm -rf .worktrees/* .specialists/jobs/*`; verified `git worktree list` clean. Killed 1 orphan `dolt sql-server` for `/tmp/bd-proto-test/`. |
| `bd close` (via specialist auto-close) skipped enforcing memory-gate ack across multiple beads | Specialist auto-close path bypasses the per-bead memory-ack kv requirement; orchestrator must ack manually after merge. | Manually ran `bd kv set "memory-acked:<id>"` for every closed bead before final session memory gate. |

## Code Changes

No commits landed on `main` this session — all work shipped via PR branches:

### PR #224 — `fix/xtrm-sxug-doctor-cwd` (xtrm-sxug)
- `cli/src/commands/doctor.ts`: new `resolveDoctorCwd()` helper; uses `findProjectRoot()` fallback; throws clear error when not inside an xtrm project.
- `cli/src/tests/doctor.test.ts`: regression test running doctor from nested subdirectory.

### PR #225 — `fix/xtrm-d3wx-status-non-interactive` (xtrm-d3wx)
- `cli/src/commands/status.ts`: adds `--check` flag and gates the inline sync prompt on `check || !process.stdin.isTTY`.
- `cli/src/tests/status.test.ts`: regression test asserts no prompt call in non-interactive paths.

### PR #226 — `fix/xtrm-ntf8-pi-global-root-fallback` (xtrm-ntf8 + xtrm-1hwe)
- `cli/src/core/pi-runtime.ts`: `resolveGlobalNpmRootDir()` (shells `npm root -g`), `resolveInstalledPiPackageJsonPath()` with agent-local-first / global-root-fallback shape, threaded `npmRootDir` through `getInstalledPiPackageVersion` / `isPackagePresentInPiAgent` / freshness / assurance / doctor providers. Plus 4 `// nosemgrep` annotations.
- `cli/src/tests/pi-runtime-safeguards.test.ts`: 3 regression tests (basic global fallback, agent-local-first wins, scoped-global never reports missing).

### PR #227 — `fix/xtrm-0kgm-install-flake` (xtrm-0kgm)
- `cli/src/core/claude-runtime-sync.ts`: hardens `resolveHooksForProjectRuntime` with `Array.isArray(wrappers) ? wrappers : [wrappers as HookWrapper]` guard.

### PR #228 — `feat/xtrm-7xxz-payload-hygiene` (xtrm-7xxz)
- `scripts/check-payload-hygiene.mjs` (new, 129 lines): runs `npm pack --dry-run --json`, applies denylist (`.xtrm/worktrees/`, `.pi/`, `.serena/`, `__pycache__/`, `*.log`, `*.db`, `*.sqlite*`, `workspace/`, `evals/`, `.specialists/jobs/`, `.specialists/db/`, `.beads/dolt/`, `.beads/backup/`, `.beads/issues.jsonl`) + independent absolute-path scan for `/home/*`, `/Users/*`, `file:///home/`, `file:///Users/`.
- `scripts/__tests__/check-payload-hygiene.test.mjs`: covers denylist-only, absolute-path-only, combined.
- `package.json`: new `check:payload-hygiene` script, wired into `prepublishOnly` chain after `check:registry-pack-parity`.

## Documentation Updates

- This report (`.xtrm/reports/2026-05-13-8a3b00e.md`).
- 3 saved memories (see below).
- No CHANGELOG entries this session — happens when PRs merge.

## Open Issues with Context

### Filed this session, awaiting CI/merge

| ID | P | Title | Status | Context / Suggestions |
|----|---|-------|--------|----------------------|
| `xtrm-sxug` | P1 | `xt doctor` cwd resolution | CLOSED; PR #224 open | Awaiting CI green + merge. |
| `xtrm-d3wx` | P1 | `xt status` non-interactive | CLOSED; PR #225 open | Awaiting CI green + merge. Note in PR body: reviewer telemetry blind spot, see unitAI-889dv. |
| `xtrm-ntf8` | P1 | Pi global root fallback | CLOSED; PR #226 open | Awaiting CI green + merge. Contains the nosemgrep annotations for the 4 path.join flagged lines. |
| `xtrm-0kgm` | P1 | install-integration flake | CLOSED; PR #227 open | Awaiting CI green + merge. Optional follow-up: dual-shape regression test for `resolveHooksForProjectRuntime`. |
| `xtrm-7xxz` | P1 | payload hygiene denylist | CLOSED; PR #228 open | Awaiting CI green + merge. Gate currently fails on 3 pre-existing leaks — by design. |
| `xtrm-fldz` | P2 | cleanup .xtrm/.../evals/evals.json from pack | open | Discovered-from xtrm-7xxz. Add `.npmignore` entry or move out of packed tree. |
| `xtrm-kuss` | P2 | cleanup .xtrm/.../workspace/.../outputs/response.md from pack | open | Discovered-from xtrm-7xxz. Same approach. |
| `xtrm-of65` | P2 | cleanup packages/pi-extensions/.serena/project.yml from pack | open | Discovered-from xtrm-7xxz. Same approach. After all 3 land, `npm run prepublishOnly` should pass end-to-end. |

### Filed in specialists repo

| ID | P | Title | Status | Context |
|----|---|-------|--------|---------|
| `unitAI-889dv` | P1 | fix(feed): sp feed returns partial tail instead of full DB replay | open | Structural reviewer-visibility bug. Repro evidence + two fix paths (fix sp feed to replay DB, OR inject tool-call summary into reviewer context). |

### Pre-existing, carried forward

Unchanged from morning report `2026-05-13-0f401e1.md`: xtrm-nogp (P0 install matrix), xtrm-2yn4 / xtrm-sn9t (P1 CI gates), xtrm-9xg2 epic + .2/.3/.4/.5 children (P1/P2 flat active layout), and the P2/P3 backlog (xtrm-2oho, xtrm-yb0u, xtrm-5sz2, xtrm-ombq, xtrm-4ud5, xtrm-o0eu, xtrm-tools-adh, xtrm-ya2i, xtrm-ya67, xtrm-kye9, xtrm-cvjg).

## Memories Saved

| Key | Content |
|-----|---------|
| `xt-doctor-now-resolves-cwd-via-findprojectroot-when` | xt doctor now resolves cwd via findProjectRoot() when --cwd omitted, then validates .xtrm/registry.json and throws explicit error otherwise. |
| `reviewer-partial-on-missing-gitnexus-impact-telemetry-is` | Reviewer PARTIAL on missing gitnexus_impact telemetry is structurally unreliable: sp feed returns only an 8-line tail, not the full DB replay. The full tool-call history lives in .specialists/db/observability.db specialist_events table. Always verify against the DB before treating 'missing telemetry' as a finding. Bug: unitAI-889dv. |
| `semgrep-pre-push-hook-flags-path-join-agentdir` | semgrep pre-push hook flags `path.join(agentDir, ..., npmPackageName, ...)` in cli/src/core/pi-runtime.ts as path-traversal even though npmPackageName comes from the xt-managed allowlist. Fix: inline `// nosemgrep` before the path.join line (generic form works). PR #226 has the precedent. Same will apply to any new managed-package path resolver code. |

## Suggested Next Priority

1. **Wait for CI on #224–#228** then run `/xt-merge` (or admin-merge in queue order: 224, 225, 227 first — small/clean; then 226 with the semgrep annotations; then 228 last as it's the biggest surface). Five sequential merges, all clean diffs. Each unblocks subsequent work.

2. **Land the 3 cleanup beads (xtrm-fldz, xtrm-kuss, xtrm-of65)** after #228 merges. These are 1-line `.npmignore` or pack-files-list edits each. Once all 3 land, `npm run prepublishOnly` passes end-to-end — releases unblock.

3. **`xtrm-tools 0.7.18`** is then cuttable. `[Unreleased]` in CHANGELOG is already substantial; this session adds 5 more entries. Cut after the cleanups.

4. **`unitAI-889dv` (specialists)** is the highest-value defensive fix in the queue — every future reviewer-vs-executor process audit relies on a correct `sp feed`. Fix path (a) (replay DB) probably wins on documentation alignment.

5. After all of the above, `xtrm-sn9t` (fresh-machine install smoke) becomes the next-most-valuable CI gate.

### Open release work parked

- `xtrm-tools 0.7.18` — blocked by the 3 cleanup beads above (otherwise prepublishOnly fails).
- `specialists v3.14.2` — independent timing.
- Fleet sweep — after both releases.

### Out-of-band notes

- Local `npm link` to xtrm-tools remains active; all worked-from-here repos pick up the new logic immediately without releases.
- `sp merge` spuriously reports "TypeScript gate failed" while actual `npm run typecheck` passes — file as friction bead if it persists.
- Direct push to `main` remains blocked. Every change ships via feature branch + PR.
- Pre-existing waiting reviewer `b4993a` (xtrm-bd52, xtrm-9xg2.2 chain — stalled since 2026-05-09) was stopped at session close. The bd beads remain open under the `xtrm-9xg2` epic; no workspace state was recoverable.
- Filesystem cleanup at session close: 52 stale `.worktrees/` dirs + 167 stale `.specialists/jobs/` dirs removed, 3 orphan jobs stopped, 1 orphan `/tmp/bd-proto-test/` dolt killed. `git worktree list` and `sp ps` running counts now clean.
