---
session_date: 2026-05-07
branch: main
commits: 1
issues_closed: 1
issues_filed: 1
specialist_dispatches: 6
models_used: ["nano-gpt/zai-org/glm-5", "openai-codex/gpt-5.4-mini", "openai-codex/gpt-5.4", "openai-codex/gpt-5.3-codex"]
---

# Session Report — 2026-05-07

## Summary

Substantial dirty state was reconciled under `xtrm-svhc` after closing `xtrm-tools-be9`. `be9` remains the narrow stale `.xtrm/skills/default` symlink repair: `scaffoldSkillsDefaultFromPackage()` now preserves only symlinks resolving to the current package payload and replaces stale/broken symlinks; targeted vitest coverage passes. The specialists default mirror migration is now explicit: `.specialists/default/` was removed so package runtime owns defaults, while project-only `parallel-review` was preserved as `.specialists/user/parallel-review.specialist.json` because the installed `@jaggerxtrm/specialists` package does not currently ship that role. `specialists list --full` now resolves common roles from `[package]` with no YAML deprecation warnings. Pi runtime state is classified: tracked `.pi/npm` symlink deletion is intentional because `.pi/npm/` is ignored local runtime state, and `.pi/settings.json` declares `npm:@jaggerxtrm/pi-extensions`. XTRM UI cleanup fixed the reviewed risks: `forceTheme` gates theme writes, compact external tool result handling obeys the preference, flattool theme names are recognized, hidden thinking always uses no placeholder, duplicate untracked theme/runtime artifacts were removed, and the global `~/.pi/agent/settings.json` compact side-effect was removed. `.xtrm/registry.json` was regenerated after retained skill/config changes. Remaining broad CLI typecheck failures are pre-existing/unrelated and documented below; the branch is still ahead of `origin/main` by `986757b`.

## Issues Closed (1)

| ID | Type | Title | Result |
|----|------|-------|--------|
| xtrm-tools-be9 | bug | xt update should repair stale skills/default npm symlink | Closed after targeted implementation and tests passed. |


## Issues Filed

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| xtrm-hlsy | P1 | bug | Recurring self-symlink: `.pi/npm -> itself`, breaks pi launch with ELOOP | Filed today to track the broader `.pi/npm` self-loop failure observed in xtrm-tools and vaultctl. The current dirty tree shows `.pi/npm` converted from tracked symlink to real npm project directory, so this issue is directly relevant before committing any `.pi` changes. |

## Problems Encountered

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| Hidden-thinking placeholder preference was mischaracterized | Cleanup introduced/mentioned a positive `showThinkingPlaceholder` preference, but XTRM UI requirement is no placeholder at all. | Removed the preference path and force `setHiddenThinkingLabel("")`; report corrected under `xtrm-2qqn`. |
| `.specialists/default` mirror shadowed package runtime | Runtime migrated to npm package defaults, but local JSON/YAML mirrors still had precedence and YAML emitted deprecation/schema warnings. | Removed the tracked default mirror and moved project-only `parallel-review` to `.specialists/user/`. `specialists list --full` is now clean and package roles resolve from `[package]`. |
| `.pi/npm` tracked symlink conflicted with local runtime directory | Historical tracked symlink no longer matches Pi package runtime behavior; `.pi/npm/` is already ignored and now contains local npm project state. | Classified tracked symlink deletion as intentional. Do not add `.pi/npm/` contents. Keep `.pi/settings.json` package declaration. |
| XTRM UI review found local config/state risks | Code-sanity/security passes flagged silent compact-flag persistence, untyped row theme helper, and cross-project global Pi settings writes. | Removed global compact side-effect, typed row theme helper, preserved prefs through session entries, restored `forceTheme` checks, and gated compaction by preference. |
| Specialist orchestration had runtime friction | Initial specialist DB file was missing, causing SQLite client unavailable; explorer later went dead/waiting after tool/API issues. | Initialized `.specialists/db/observability.db`, consumed available code-sanity/security/reviewer outputs, and documented job IDs. Waiting/dead specialist sessions are cleanup-only. |
| Final `parallel-review` failed | The preserved user-only `parallel-review` specialist uses `anthropic/claude-sonnet-4-6`, but Pi has no active Anthropic provider/API key in this environment. | Recorded as orchestration evidence only. Code-sanity, security-auditor, and reviewer outputs were consumed; no active jobs remain. |
| Full validation has pre-existing failures | Targeted be9 tests pass, but broader typecheck still fails on unrelated existing TypeScript errors in prompts typings, debug.ts implicit anys, context.ts Conf typing, interactive-plan/preflight types, and repo-root async condition. | Treat targeted validation as green for this scope; do not block runtime migration cleanup on unrelated failures. |


## Code Changes

### Change summary
- Added: 3 intentional untracked paths remain: two canonical flattool themes and this report. Runtime junk (`.beads/push-state.json`, nested `.serena/`, duplicate theme copy, backup file) was removed/ignored.
- Modified: tracked runtime/config/skill/report files after be9 and dirty-state cleanup.
- Deleted: tracked `.specialists/default/` mirror and tracked `.pi/npm` symlink intentionally removed for package/runtime ownership.

### Pi extension/UI work

- `.xtrm/packages/pi-extensions/extensions/xtrm-ui/index.ts` now expands XTRM UI preferences with `toolRowBg` and `compactExternalToolResults`; adds flattool theme resolution; keeps compact-tool preferences in Pi session entries without mutating global `~/.pi/agent/settings.json`; and registers `/xtrm-ui-rowbg` plus `/xtrm-ui-compact-tools` commands.
- The same file moves external tool-result compaction into `xtrm-ui` for all non-builtin tools, including generic GitNexus/MCP-style labels and hierarchy formatting. Builtin Pi tools keep custom renderers with `renderShell: "self"` and optional row backgrounds.
- Hidden thinking behavior is patched through Pi's `AssistantMessageComponent` so hidden thinking can render without placeholder text while expanded messages can still reveal content.
- `.xtrm/packages/pi-extensions/extensions/pi-serena-compact/index.ts` is intentionally made passive so Serena/GitNexus result compaction is not double-applied.
- `.xtrm/packages/pi-extensions/package.json` bumps `@jaggerxtrm/pi-extensions` from `0.7.10` to `0.7.17`; `.pi/settings.json` now declares `npm:@jaggerxtrm/pi-extensions`.
- Theme work adds canonical `pidex-*-flattools.json` files and changes dark tool success/error backgrounds from `surfaceAlt` to `surfaceMuted`.

### be9 symlink repair

- `cli/src/core/registry-scaffold.ts` now compares the real path of `.xtrm/skills/default` symlink targets against the current package payload. Only symlinks resolving to `packageRoot/.xtrm/skills/default` are preserved; broken or valid-but-stale symlinks are removed and replaced with a copy of the current package payload.
- `cli/src/tests/registry-scaffold.test.ts` adds regression coverage for replacing stale valid symlinks while preserving current-package symlinks.
- `cli/src/tests/install-runInstall.test.ts` keeps runInstall coverage aligned with the new current-package symlink semantics.
- Validation: `cd cli && npx vitest run src/tests/registry-scaffold.test.ts -t scaffoldSkillsDefaultFromPackage` passed (6 tests); `cd cli && npx vitest run src/tests/install-runInstall.test.ts` passed (3 tests).

### Specialist/runtime mirror state

- `.specialists/default/` is intentionally removed so the npm package runtime owns defaults. Project-only `parallel-review` is preserved under `.specialists/user/`; `specialists list --full` has no YAML deprecation warnings.
- `.xtrm/registry.json` is mostly a version sweep from `0.7.16` to `0.7.17` across managed assets, plus any hashes created by current file edits.

### Committed change ahead of origin

- `986757b fix(agents-top): hooks terminology and xt claude worktree entry` updates `.xtrm/config/instructions/agents-top.md`; branch is `main...origin/main [ahead 1]`.

## Documentation Updates

- `skills/session-close-report/SKILL.md` and `.xtrm/skills/default/session-close-report/SKILL.md` now prefer one same-day SSOT report, add mandatory pre/post cleanup verification, and require documenting cleanup failures.
- `.xtrm/skills/default/using-specialists-v3/SKILL.md` gains mandatory `specialists list --full`, specialist file-layer guidance, `sp edit` requirements, paranoid orchestration discipline, security/code-sanity chain rules, monitoring sleep timers, and worktree cleanup rules.
- `.xtrm/skills/default/sync-docs/SKILL.md` fixes the drift detector JSON query from `.[] | select(.path == ...)` to `.stale[]? | select(.doc == ...)`.
- This report was generated because no same-day report existed: `.xtrm/reports/2026-05-07-986757b.md`.

## Open Issues with Context

### Ready for next session

| ID | P | Title | Status | Context / Suggestions |
|----|---|-------|--------|----------------------|
| xtrm-hlsy | P1 | Recurring self-symlink: `.pi/npm -> itself`, breaks pi launch with ELOOP | open | Newly filed today. Use this to track root cause rather than burying it in `be9`. Suggested approach: add a doctor check that detects self-loop symlinks under `.pi/`, then reproduce whether xtrm's symlink/copy helpers or Pi's `ensureNpmProject` creates the loop. Current working tree has `.pi/npm/` as a real directory, not the tracked symlink. |
| xtrm-etp4 | P1 | CI: fail on stale specialists skill mirror or registry | open | High-value next guard. The current dirty tree proves this is needed: the prior stale/missing `.specialists/default` mirror and registry drift were visible locally. Implement a CI command that vendors specialists skills, regenerates registry, and fails on diff for `.xtrm/skills/default` + `.xtrm/registry.json`. |
| xtrm-jpjl | P1 | CI: validate xtrm-tools npm package contains managed payload | open | Pair with `xtrm-etp4`. Use `npm pack --dry-run --json` assertions for required `.xtrm/registry.json` and default skills so source fixes actually ship in the package. |
| xtrm-sn9t | P1 | CI: fresh-machine xt install/update smoke with packed specialists | open | Best validation for the current failure class. Build a temp-prefix smoke using packed xtrm-tools and specialists, then run `xt install`/`xt update --apply`/`xt doctor` to catch stale symlink, package payload, and mirror issues before release. |
| xtrm-tools-u3t | P1 | xtrm-tools@0.7.12 wrapper: xt init silently skips skill sync (findRepoRoot broken) | open | `bv --robot-triage` top pick due age and P1. After current dirty state is stabilized, confirm whether this old wrapper bug is still reproducible on latest; close as obsolete only with evidence, otherwise patch wrapper/root detection. |

### Backlog / contract hardening

| ID | P | Title | Status | Context / Suggestions |
|----|---|-------|--------|----------------------|
| xtrm-9xg2 | P1 | Epic: harden xtrm-tools ↔ specialists release contract | open | Parent epic for mirror, registry, package, and fresh-machine gates. Current dirty state is a concrete example of why split-repo contract checks are required before considering monorepo. |
| xtrm-cvjg | P2 | CI: handle specialists repository_dispatch release-candidate validation | open | Add after local CI gates exist. It should vendor from a specialists release-candidate SHA/tarball and report pass/fail back to specialists. |
| xtrm-pf8h | P2 | Release gate: record specialists source/package used for vendored payload | open | Needed to make registry/default-skill payload traceable. Could be a generated manifest or registry metadata containing specialists package version/SHA. |
| xtrm-tools-adh | P2 | xt init/install: registry.json model — per-repo copy vs. user-scope + marker | open | Related to update discoverability. Recommendation in issue remains Option A first: copy registry on init/install to unblock bulk update, then consider user-scope registry + marker. |

## Memories Saved

Saved/updated project memory via OpenWolf and bd notes for the specialists npm runtime migration, be9 symlink repair, and Pi UI cleanup findings.

## Suggested Next Priority

1. Commit in split slices, not one blob: (a) `be9` symlink repair/tests, (b) specialists package-runtime migration, (c) Pi runtime/settings, (d) XTRM UI/theme changes with regenerated registry, (e) docs/report/OpenWolf artifacts.
2. Before commit, run `gitnexus_detect_changes({scope:"staged"})` on each staged slice and keep unrelated typecheck failures documented.
3. Follow up separately on broad CLI typecheck debt and the old xt worktree `.xtrm/worktrees/xtrm-tools-xt-pi-install` if it is no longer needed.
