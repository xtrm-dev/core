---
session_date: 2026-05-14
branch: main
commits: 0
issues_closed: 0
issues_filed: 1
specialist_dispatches: 0
models_used: []
---

# Session Report — 2026-05-14

## Summary

Diagnostic-only session. Triaged a regression in `@jaggerxtrm/specialists` v3.15.0 where `sp run --background` silently dies after `payload_breakdown` (seq=4) and never reaches the first agent turn — the operator reported the symptom; this session traced provenance, reproduced the failure cleanly, isolated the root cause, and filed an upstream P0 bead with a regression-catching acceptance criterion. **Root cause:** commit `fa00c5ed` ("fix: stabilize background run integration", `dev/specialists`, May 14 02:46Z) wrapped the inner tmux command as `/bin/bash -lc 'cd <cwd> && exec ${cmd}'`. The `-lc` (login + non-interactive) forces bash to rebuild PATH from `/etc/profile` + `~/.bash_profile` only — `~/.bashrc` (where NVM init puts `pi` on PATH on this and most setups) is **not** sourced. Result: `spawn('pi', args, ...)` in `src/pi/session.ts:669` throws `ENOENT` mid-supervisor; tmux session terminates within milliseconds; obs DB has exactly 4 events per failed run. **Provenance:** the change was bundled into the same commit that closed `unitAI-sxmmy` (P2, test fix — Supervisor's DB bootstrap) and `unitAI-dq6vr` (P3, cosmetic "job ID not yet available" warning race). The `-lc` was a side effect of the wrapper hardening (intent: disambiguate `exec cd ...` parsing as the `cd` builtin) — but `-c` alone would have sufficed; `-l` was the unnecessary addition. **Gates that missed it:** the integration test (`tests/integration/cli/run.integration.test.ts -t background`) only verifies handoff file + readStatus(), never an actual agent turn. Reviewer audit on `unitAI-sxmmy` returned PASS 91 with that same blind spot. CI inherited the gap. CHANGELOG entry talked about fixing the cosmetic warning, not the wrapper change. Filed `unitAI-baz0t` (P0, specialists) with the full root-cause analysis, three fix paths (revert `-lc` → just `-c` / resolve `pi` to absolute / forward PATH via tmux `-e`), and a proposed acceptance criterion: `sp run --background` must (a) write `turn:start` (seq ≥ 5) to obs DB within 30s AND (b) keep an `sp-*` tmux session visible — both together. Zero xtrm-tools-side code changes this session.

Note on the 12 commits visible on `origin/main` since yesterday's `8a3b00e`: those belong to the **2026-05-13 auto-mode stabilization** orchestrator (PRs #224–#228, #231–#236) and are covered in `.xtrm/reports/2026-05-13-8a3b00e.md`. They land via the same-day SSOT for 2026-05-13, not this 2026-05-14 report.

## Issues Filed (1, cross-repo)

| ID | Repo | P | Type | Title | Why |
|----|------|---|------|-------|-----|
| `unitAI-baz0t` | dev/specialists | P0 | bug | fix(run): --background dispatch broken — '-lc' wrapper strips PATH; pi spawn ENOENT | Real release blocker shipped in specialists v3.15.0. Operator caught it via attempted dispatch ("after the latest specialists update, the tmux --background mode does not work"). Bead carries full root-cause + reproduction + three fix paths + proposed regression-catching acceptance test. |

Also filed in this repo as session bookkeeping:

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| `xtrm-qlic` | P3 | chore | chore(session): 2026-05-14 session close | This bead; tracks the close-out work. |

## Problems Encountered

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| `sp run --background` dispatches accept the call, return a job ID, fire 4 obs-DB events (run_start, meta, meta, payload_breakdown), then die. No `tmux ls` shows the `sp-*` session at any inspection point. | Inner tmux command is `/bin/bash -lc 'cd <cwd> && exec <node sp-cli>'`. `-lc` is login + non-interactive: sources `/etc/profile` + `~/.bash_profile` only, NOT `~/.bashrc`. PATH drops to `/home/dawid/.local/bin:/usr/local/bin:/usr/bin:/bin:/usr/local/games:/usr/games`. `pi` lives at `~/.nvm/versions/node/v24.15.0/bin/pi` — off-PATH. `spawn('pi', args)` ENOENTs in `pi/session.ts:669` → supervisor crashes pre-prompt → tmux session ends with the shell. | Filed `unitAI-baz0t`. Manual reproduction with bare `tmux new-session -d -s probe -c /tmp "/bin/bash -lc 'echo PATH=$PATH; which pi'"` shows empty `which pi`; same with `/bin/bash -c '...'` shows `pi` resolved. Captured ENOENT stack trace via the same manual reproduction. |
| Foreground `sp run` from this same node v24.15.0 environment worked fine (job `8d1e8a` reached 3 successful turns before I killed it via the test `timeout 60`). | Foreground inherits the operator's interactive shell env (full PATH including NVM-injected paths). The `-lc` wrapper only fires on background dispatch. | Used the foreground/background asymmetry to confirm the regression is in the wrapper, not in pi/specialists installation. |
| Initial assumption that `-lc` was equivalent to `-c` + env-stable. | Login-shell mode discards inherited PATH and rebuilds from system profile files. Operators relying on `~/.bashrc` (typical NVM setup) lose tools on PATH. | Documented as the central insight in `unitAI-baz0t`. Recommend revert to plain `-c` in the upstream fix. |

## Code Changes

**Zero xtrm-tools code changes.** Session was diagnostic + cross-repo bead filing. Cleanup of `/tmp` test artifacts (`sp-trace.log`, `lc.out`, `pe.out`, `direct.out`, `probe-events.ts`, `sp-debug-test.out`, `probe.out`, `test-out.log`, `vitest-*.log`, `ci-fail.log`, `pr206-lock.json`, `doctor.out`) plus throwaway `/tmp/bd-proto*` and `/tmp/xt-x80f-test` repos performed pre-report.

The 12 commits visible on `origin/main` since yesterday's `8a3b00e` (PRs #224–#228, #231–#236 — doctor cwd fix, xt status non-interactive, Pi global root fallback, install-integration flake fix, payload hygiene denylist, pack absolute-path scrub, pack exclusions, `.pi/npm` gitignore, pi-runtime perf) all belong to the 2026-05-13 auto-mode session. See `.xtrm/reports/2026-05-13-8a3b00e.md`.

## Documentation Updates

- `unitAI-baz0t` (specialists): description carries full root-cause analysis with PATH-comparison evidence, ENOENT stack trace, three concrete fix paths (smallest = drop `-l`; hardening = absolute-path resolve; thorough = explicit env forward via tmux `-e`). Notes section appended later with the proposed regression-catching acceptance criterion (turn:start in DB + tmux session alive, both within 30s).
- This report: `.xtrm/reports/2026-05-14-e4600bc.md`.

## Open Issues with Context

### Ready for next session (upstream specialists work)

| ID | Repo | P | Title | Context / Suggestions |
|----|------|---|-------|----------------------|
| `unitAI-baz0t` | specialists | P0 | --background dispatch broken | Fix path A is a one-character revert (`-lc` → `-c`) in `src/cli/run.ts` where `tmuxCmd` is constructed. Then rebuild `dist/index.js`, smoke `sp run explorer --prompt 'hi' --background` against the proposed acceptance criterion (turn:start in DB within 30s + `tmux ls` shows the sp-* session). Recommended companion: add the acceptance check as a real integration test in `tests/integration/cli/run.integration.test.ts` so this regression cannot recur. Bonus hardening (B): in `src/pi/session.ts:669`, resolve `pi` to absolute path via `which pi` at session-factory time and pass `piBin` instead of bare `'pi'` — defends against any future PATH issue across all spawn paths. |

### Ready for next session (xtrm-tools side)

Most yesterday-audit priorities landed via auto-mode (PRs #224, #225, #226, #227, #228). What's left:

- **B2** — `xt doctor` reports `scripts/scaffolder.py` as drifted despite the file matching origin/main. Drift baseline stale somewhere; not investigated yet.
- **B3** — `xt doctor activeReady=false` from the local stale `.xtrm/skills/active/gitnexus/` real-dir leftover. Local-only on this machine; proper fix is auto-clean in `xt init/update` (the `xtrm-9xg2.3` epic child).
- **C2** — `xtrm-sn9t` fresh-machine `xt install/update` smoke is the missing CI layer that would let `/releasing` flip from operator-careful to CI-gated.

### Backlog (unchanged)

`xtrm-2yn4` (release-gate wiring, blocked by C2), `xtrm-nogp` (install order matrix), `xtrm-9xg2.{2,3,4,5}` (flat active layout work), `xtrm-o0eu` (`@jaggerxtrm/xtrm` rename epic), `xtrm-ya2i` (`xt init` seed `registry.json`), `xtrm-ya67` (`upsertManagedBlock` orphan duplicates `xtrm:start`), `xtrm-kye9` (managed GitNexus post-commit hook).

## Memories Saved

| Key | Content |
|-----|---------|
| (none new) | Diagnostic session. The root-cause insight (PATH-strip via `bash -lc` login-shell mode) is captured in `unitAI-baz0t` description plus the upstream changelog note that will land with the fix. Saving as a bd memory now would duplicate the bead; the bead is the more discoverable surface for the next agent who hits the same symptom (`bd memories <keyword>` matches bead descriptions). |

Existing memory `direct-push-to-main-is-blocked-as-of` (saved 2026-05-13) remains relevant — every change in this and the previous session went through PRs.

## CHANGELOG sync

**Skipped — no user-facing change shipped to xtrm-tools this session.** The cross-repo bead in specialists tracks the operator-visible issue. When upstream `unitAI-baz0t` lands and a specialists patch is cut, that release's CHANGELOG carries the fix entry; xtrm-tools-side picks it up via `xt update` after the specialists release publishes.

Per the session-close-report skill rules:
> "No user-facing change shipped (pure orchestration, doc-only edits to internal-handoff files like reports/skills, refactors with no observable effect): skip — do not pollute `[Unreleased]` with internal noise."

This session qualifies — diagnosis + cross-repo bead filing, zero xtrm-tools surface change.

## Suggested Next Priority

1. **Unblock `sp run --background` (`unitAI-baz0t`, P0).** Fix A in dev/specialists takes ~1 minute (single-char edit + dist rebuild). Without this, every operator on specialists v3.15.0 has no working background dispatch — auto-mode orchestration and any concurrent specialist work is blocked unless they downgrade.
2. **After (1), retroactively add the proposed regression test** to `tests/integration/cli/run.integration.test.ts`. The acceptance check spelled out in `unitAI-baz0t` notes — `turn:start` in obs DB + tmux session alive — catches every future variant of the same class of bug (env-strip, cwd-corrupt, exec-builtin-shadow, etc.) in one test.
3. **Cut specialists patch release** (v3.15.2 or whatever next) after (1)+(2). Until then, this machine's local specialists checkout is at v3.15.0 and `--background` is unusable.
4. **Continue stabilization items B2 / B3 / C2** from yesterday's 2026-05-13c audit. None block (1)–(3); pick up whenever specialists is unblocked.

The release-gate workstream (`xtrm-sn9t` + `xtrm-7xxz` companions) is on hold by operator preference until more cheap stabilization lands. That decision stands.
